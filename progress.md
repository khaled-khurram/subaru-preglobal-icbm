# Poor-Man's Longitudinal Control — Project Progress

> Living memory / RAG anchor for the build. Keep entries terminal and factual.
> Update the **Status**, **Changelog**, and **Open Questions** every working session.

**Vehicle:** 2015 Subaru Outback 2.5i Limited, EyeSight
**Platform:** `SUBARU_OUTBACK_PREGLOBAL` (fingerprint confirmed)
**Hardware:** Comma 4
**Software:** Personal fork `github.com/khaled-khurram/sunnypilot` (renamed from initial `PoorMansLong`), branch `phase1-curve-advisory`, built off sunnypilot's official `release-mici` tip (`af744c8` / `sunnypilot v2026.002.001`, confirmed current as of 2026-07-20 — not stale, "002" is not a month, it's sunnypilot's own version-train numbering)
**Doc created:** 2026-07-19
**Last updated:** 2026-07-22
**Current phase:** 🟢 **Phase 1 stable and effectively closed.** Speed-limit display was tried, disliked by the user even after two rounds of sizing/alignment fixes, and **removed entirely** (commit `53ac87c`) — kept the alert/HUD z-order fix (a real bug, unrelated to speed-limit) and the Phase 2 button decoder. **Phase 2's Q4 (ACC button CAN bits) is now confirmed twice over** — once statistically (412 archived events) and once via a live, deliberate, real-time test the user ran themselves (12 presses, 12 correct detections, zero misses/false positives) — see §7 Q4 and §11. See §5 for Phase 2 hardware confirmation (no new hardware needed), §9 for the Tailscale plan (not yet installed, on hold per user request).
**UI stack:** raylib/Python — `system/ui/lib/application.py` (`gui_app`), onroad layout is `MiciMainLayout` (compact-screen variant, matches `release-mici`). Zero Qt in the tree.
**UI caveat (found 2026-07-20):** mici hardware's on-device Toggles screen never exposed a `SmartCruiseControlMap` toggle at all (that only exists in the desktop/tici multi-tab settings, which mici doesn't route to) — same story on the sunnylink web dashboard, which mirrors the desktop gating. There is currently **no persistent UI control** for `SmartCruiseControlMap` or `CurveSpeedAdvisory` on this hardware; both must be set directly via the on-device params files over SSH, and will need to be re-set after any factory reset / params wipe.

---

## 1. Vision (the one-paragraph pitch)

EyeSight can't be commanded on gas/brake over CAN on the pre-global platform — that's settled (see §7). So instead of *replacing* longitudinal control, **ride it**: let the comma's vision model decide the right speed (for lead cars EyeSight loses lock on, and for curves/limit changes), then have openpilot **spoof the steering-wheel ACC buttons** to move EyeSight's *own* setpoint down/up. EyeSight does the actual braking; we just turn its dial. "Poor man's" longitudinal because it inherits EyeSight's floor (~25 mph, can slow but not stop) and EyeSight's decel rate — but it removes the constant manual intervention.

**Guiding principle:** validate the *decision* before automating the *actuation*. Phase 1 puts a human thumb in the loop as the actuator so the speed-target logic can be proven with zero risk to the car.

---

## 2. Key facts / constraints (RAG anchors — stable truths)

- Pre-global exposes **no** openpilot gas/brake actuation. Direct CAN longitudinal is undecoded and unattempted community-wide (Aug 2023 bounty still unclaimed as of last check).
- Lateral works only via the community Subaru branch; steering is **torque-based** with a max-torque cap.
- EyeSight stock ACC **minimum set speed ≈ 25 mph** (CONFIRM exact value on car). Below that, EyeSight ACC drops out → **driver must brake for anything requiring < ~25 mph**. This bounds the whole feature: *it slows the car, it does not stop the car.*
- Decel aggressiveness after a setpoint change is **EyeSight's, not tunable by us**. We only control *when* and *by how much* the setpoint moves, and the *cadence* of injected button events.
- OSM map data is served on-device by the **`mapd`** daemon (pfeiferj lineage). Region download enables map-based speed-limit + curve features. Full continental US dataset confirmed on-device, 5.4 GB, current as of last check.
- sunnypilot already ships the two relevant features on long-enabled cars:
  - **MTSC** (Map Turn Speed Control, "SCC-M") — target speed from **OSM curvature**, looks *ahead beyond sensor range* → primary source for advance warning.
  - **VTSC** (Vision Turn Speed Control, "SCC-V") — target speed from the **model's predicted path curvature**; a `VisionTurnController` estimates lateral accel and computes `v_turn`. Near-field only (sees what's currently ahead) → good cross-check, no map dependency.
- **Confirmed via source (2026-07-20):** `plannerd`'s SmartCruiseControlMap/Vision computation runs **unconditionally**, gated only on `carControl.enabled` (system engaged) — not on `CP.openpilotLongitudinalControl`. This is why Phase 1 is possible at all on a lateral-only car: the speed-target math genuinely runs live regardless of actuation capability.
- **Confirmed via source (2026-07-20):** sunnypilot's `ui_state.py` `_enforce_constraints()` force-deletes `SmartCruiseControlMap`/`SmartCruiseControlVision` params on any car lacking both long control and ICBM — a *product-level* UI gate, unrelated to what the underlying computation actually needs. Patched in our fork (see Phase 1 below) since it doesn't apply to a read-only advisory use case.
- **Confirmed via sunnypilot founder, community forum, 2026-07-20:** ICBM (Intelligent Cruise Button Management — sunnypilot's own button-spoofing feature, exactly our Phase 3 idea) is **unimplemented for all Subaru platforms**, not just ours. Quote: *"The necessary button message structure and routing aren't exposed or implemented by Subaru in a way that supports emulation yet... this isn't something we're actively pursuing... would likely need to be a community-driven effort."* Also mentioned: *"previous POCs using actuation test commands via UDS in diagnostic sessions, but those were never explored further"* — a genuinely new avenue for Phase 2 (see below). Source: https://community.sunnypilot.ai/t/enabling-icbm-on-17-impreza/191

---

## 3. The physics we're reusing (so the doc is self-contained)

Safe/comfortable curve speed is set by **target lateral acceleration**, *not* steering torque (common confusion — torque is actuator authority, a separate ceiling):

```
v_max = sqrt(a_lat_target / κ)          κ = curvature = 1/R
```

- Comfort target `a_lat_target` ≈ **1.8–2.5 m/s²** (what MTSC/VTSC use; tune to taste).
- Curvature κ comes from OSM way geometry (MTSC) or the vision path (VTSC).
- Torque cap matters *separately*: if a curve needs more steering torque than the community branch is allowed to command, lateral control runs wide regardless of speed. Worth logging as a distinct failure mode, but it does **not** drive the speed number.

---

## 4. Architecture (tandem model)

```
[mapd / OSM curvature]  ─┐
                         ├─► speed-target calc (reuse MTSC/VTSC math)
[vision model path]     ─┘         │
                                   ▼
                    target_v + distance_to_event
                                   │
        Phase 1 ───────────────────┼─────────────── Phase 2/3
        emit ADVISORY                       emit BUTTON-SPOOF cmd
   (alert text + audio: "curve,          (injected ACC set-/res+ frames
    reduce to 35 in 400 ft")              → EyeSight moves its setpoint)
                                   │
                                   ▼
                          EyeSight does the braking
```

---

## 5. Phase plan + acceptance criteria

### Phase 0 — Recon & baseline (no code)  ·  STATUS: in progress (3/4 closed)
- [x] Onroad UI confirmed **raylib/Python**, `MiciMainLayout` (compact-screen variant). No Qt anywhere in tree.
- [x] `mapd` + `mapd_manager` confirmed running (~7% CPU). OSM coverage confirmed generous — full continental US dataset downloaded, 5.4 GB on disk.
- [x] EyeSight ACC min set speed confirmed **25 mph**. Button is a momentary two-stage center rocker: **stage 1 = ±1 mph per click, stage 2 = ±5 mph per click**. Press-and-hold ramps by 5 but is *slower* than manually spamming the two-stage clicks — i.e. rapid discrete presses beat holding. Relevant for Phase 3 injected-frame cadence design.
- [ ] Baseline drive: log a route where he *would* intervene, to define what "correct advisory" looks like. **Effectively merging with Phase 1's validation drive** — same test now serves both.

### Phase 1 — Advisory MVP (read-only, ZERO actuation)  ·  STATUS: implemented + deployed, pending validation drive
Human is the actuator. Screen/audio says "slow to X, curve in Y ft"; driver thumbs the button.

**Built (2026-07-20), on fork branch `phase1-curve-advisory`:**
- `cereal/custom.capnp`: new `curveSpeedAdvisory` event, new `distance` field on `SmartCruiseControl.Map`
- `map_controller.py`: MTSC now tracks live distance-to-curve every frame (previously never published)
- `curve_advisory_helper.py` (new): rising-edge trigger on MTSC's `turning` state — fires once per curve, not continuously. Gated on a **~35 mph minimum speed** (added after realizing OSM curvature doesn't distinguish a highway sweeper from a city roundabout — avoids in-town chatter)
- `longitudinal_planner.py`: wires the helper in, publishes distance
- `events.py`: "Curve Ahead" / "reduce to X mph in Y ft" alert text, `promptSingleHigh` chime, reusing the existing alert/event pipeline (no custom HUD widget)
- `alert_renderer.py` (mici): banner renders at medium height, not full-screen
- `params_keys.h` + `CurveSpeedAdvisory` param: default **off**, no on-device UI toggle yet (SSH/params-file only — see UI caveat above)
- `ui_state.py` fix: stopped `SmartCruiseControlMap`/`SmartCruiseControlVision` from being force-wiped on cars without long control or ICBM — verified live on-device that the param now survives (previously any SSH-set value was deleted almost immediately by this constraint)

**Deploy notes:** device installed via `install.sunnypilot.ai/fork/khaled-khurram/release-mici` (confirmed working URL format — no repo name needed, just `<username>/<branch>`), then switched to `phase1-curve-advisory` via SSH `git checkout` (no reinstall needed for a same-fork branch switch — only switching *forks* costs a factory reset + calibration/torque relearn, confirmed against sunnypilot's own community guidance). Device's fetch refspec was widened from single-branch to track all branches for future updates.

**Currently set on-device:** `SmartCruiseControlMap=1`, `CurveSpeedAdvisory=1` (both confirmed via direct params-file read). Visual confirmation while driving: the existing "SCC-M" badge (bottom of screen) lights up when the map controller has an active curve locked — check that first as the "is this actually alive" signal, independent of whether the advisory banner has fired yet.

- **Accept when:** on a known curvy route, the advisory fires early enough to react, with a target speed that matches what he'd manually pick, ≥ ~80% of the time, with tolerable false-positive rate. **Not yet evaluated against a real drive — this is the next step.**

### Phase 2 — Passive actuation recon (still no TX)  ·  STATUS: underway — button bit positions empirically confirmed (2026-07-22)
- **Button CAN bits confirmed (2026-07-22), see Q4.** `SET_BUTTON` = `0x144` byte0 bit3, `RES_BUTTON` = `0x144` byte0 bit4 — found by mining the already-synced local route archive (see §9 for where that lives), zero new capture drive needed. 412 real-world correlated events, not a guess.
- **Hardware confirmed (2026-07-22): no new purchase needed.** comma 4 has the panda's chip (same STM32H7 as the standalone red panda) built directly into the device, carried forward from comma 3X — no separate panda box required, unlike comma 3 on CAN-FD cars. Sources: comma's own shop pages for "comma four" and "panda". Car-specific harness already installed/driving. Both Phase 2's passive capture and Phase 3's eventual TX both run through hardware already owned; the real blocker stays the safety-firmware TX allowlist (software, not hardware — see Q5).
- Passive CAN capture (cabana) of the real steering-wheel ACC button frames: **message ID, byte layout, which bus, repeat rate**. First move before a capture session: check whether the button message is already partially decoded in opendbc's Subaru DBC (read-only, e.g. for detecting user button presses) even though sunnypilot confirmed it's not wired up for *emulation* — could shortcut message-ID discovery.
- **New avenue (from forum research):** investigate UDS diagnostic-session actuation test commands (e.g. `InputOutputControlByIdentifier` / service `0x2F`-style) as an alternate path to button emulation, separate from spoofing the production CAN message. Sunnypilot mentioned an abandoned POC here — unclear how far it got or what Subaru-specific service IDs/security access it would need. Needs its own research pass.
- Determine whether that address is in the `SAFETY_SUBARU_PREGLOBAL` panda TX allowlist. If not → panda safety mod required (this is the "could brick / lose safety cert" boundary).
- Determine whether EyeSight accepts an **injected** button frame identically to a physical press (may fault instead) — likely needs to wait for Phase 3's bench testing to actually answer.

### Phase 3 — Closed-loop button spoofing (actuation)  ·  STATUS: all three features (curve/lead/SLF) implemented, pushed, deployed, live-armed on the device — **unaffected** by the events below, which were scoped entirely to a separate UI layer. Status-dots + one-shot override-alert UI layer shipped 2026-07-26, then **fully reverted the same evening** after causing repeated real `commIssue`/TAKE CONTROL disengages on a 3-hour highway drive (commits `e17e87a`/`539367e`, new HEAD `539367ee6b` on `phase3-actuation`, reboot-verified clean) — see the 2026-07-26 (evening) changelog entry and Q13 for the full investigation: ~90% confidence the code caused it, ~30-35% confidence in the specific mechanism first proposed. Not safe to re-attempt without a redesigned data-transport approach — see Q13's closing note. Separately, an unrelated ADSP audio/camera fault surfaced during the same-day earlier short test session and remains open — see Q12.
- Bench/parked first, then empty-road, incremental. Never below EyeSight's floor without driver takeover.
- **Hard safety rails:** preserve stock AEB/FCW; driver override always wins; fail safe to "hands to driver."
- **Accept when:** the "lead car braking, EyeSight has no lock" highway case is handled without manual intervention, repeatably and predictably.
- **Design drafted (2026-07-23):** `research/phase3_controller_design.md` — policy (threshold/deadband, shallow-only for v1), rate limiting (parameterized pending the sustained-press test), driver-override as a latching first-gate (not per-frame), architecture split between `plannerd` (decision) and `carcontroller.py` (execution) connected by a freshness-gated file (not a new `Params` key — known crash landmine), staged rollout (dry-run → empty lot/private road → supervised backroad → broader use).
- **Unblocked (2026-07-24):** the v3 live test drive (see §10 changelog) closed Q10 and exercised all three previously-untested commanded-button behaviors (deep-SET, burst, RESUME) in one drive over Tailscale (live-monitored, not blind-armed) — every one fired clean, no CAN fault, no lingering dash fault. Real controller implementation against `phase3_controller_design.md` can now proceed on solid empirical footing rather than assumption. One open thread from the same drive: a benign-looking `commIssue` alert (already-characterized process-watchdog blip, not button-specific) coincided with one of the three test moments, timing-correlated with a real driver disengage rather than the commanded press itself — not blocking, but worth a clean eye next time it's seen.

---

## 6. Difficulty read — building the UI

The honest version:

- **Custom on-screen HUD widget from scratch:** *moderate–hard right now*, because openpilot's UI is mid-migration Qt/C++ → raylib/Python. Building a bespoke widget means building on a moving target.
- **Advisory via the existing alert/event system:** *easy–moderate*, and it's the right MVP — confirmed correct in practice. Emit an event with the computed target speed + distance; the alert text + chime already render on whatever UI stack is live. Pure Python in the controls/planner layer, no C++, no rendering code. Phase 1 shipped with **zero new widget files** — the whole implementation was event/text wiring into infrastructure already composed on-screen.

Net: the *speed-target math* was largely done already (MTSC/VTSC exist and run regardless of long control). The real Phase-1 work ended up being: plumbing their output to an advisory, working around a product-level settings gate that assumed every car either has long control or ICBM, and tuning trigger thresholds (min speed, once-per-curve debounce). All tractable. The hard, risky novelty is still all in Phase 2/3.

---

## 7. Open questions / unknowns to resolve

| # | Question | Needed for | Status |
|---|----------|-----------|--------|
| Q1 | `release-mici` onroad UI: Qt or raylib? | Phase 0/1 | **CLOSED** — raylib, `MiciMainLayout` |
| Q2 | `mapd` region coverage on his routes? | Phase 1 | **CLOSED** — full CONUS dataset, 5.4GB, current |
| Q3 | EyeSight min set speed + button step + hold behavior? | Phase 1/3 | **CLOSED** — 25 mph floor; 2-stage rocker (±1 / ±5); spam beats hold |
| Q4 | Steering-wheel ACC button CAN msg ID / bytes / bus / rate? | Phase 2 | **CLOSED, doubly confirmed (2026-07-22).** Message: `CruiseControl` @ `0x144`, main bus, 50ms/20Hz. **Round 1 — statistical:** mined the local route archive (~1225 segments, no comma-device access needed) for every real cruise-set-speed change (412 found), correlated against raw `0x144` bytes. Result: `RES_BUTTON` = byte0/bit4 (93-91%, 262 events), `SET_BUTTON` = byte0/bit3 (95-100%, 150 events). Matches opendbc's own DBC exactly, corrects an earlier wrong "bits 48/49" claim from initial research. **Round 2 — live, deliberate, real-time confirmation:** wrote `tools/phase2/live_button_decoder.py` (RX-only, prints/logs a detection the instant it sees the bit flip), ran it while the user pressed a known button sequence in real time. 12 total presses across two test batches, 12 correct detections, zero missed presses, zero false positives, correct direction every time. **One interesting physical finding:** a firm stage-2 press occasionally registered as two rapid pulses of the same bit (~0.4s apart) while light stage-1 taps produced exactly one — consistent with the two-stage rocker mechanically passing through the stage-1 contact point en route to stage-2. No separate bit for click magnitude (1mph vs 5mph) — matches the archive analysis's same finding. One unexplained bit (byte 1, bit 0) correlates with BOTH directions equally in the statistical pass — almost certainly a rolling counter/heartbeat, not a button signal. |
| Q5 | Is that address TX-allowed in `SAFETY_SUBARU_PREGLOBAL`? | Phase 2/3 | **CLOSED** — confirmed directly in `opendbc/safety/modes/subaru_preglobal.h`: `0x144` is **not** in `SUBARU_PG_TX_MSGS` / `SUBARU_PG_COMMON_TX_MSGS` / `SUBARU_PG_STOP_AND_GO_TX_MSGS`. A safety-firmware change would be required to ever transmit on it. **Follow-up (2026-07-23):** a draft patch adding `0x144` to `SUBARU_PG_COMMON_TX_MSGS` was written, modeled on Honda's `0x296` (SCM_BUTTONS) TX-allowlist precedent — **not applied, not committed, not built, not flashed.** Two real open design questions flagged rather than guessed at: whether an injected frame needs to mirror the real ECU's live state fields (`cruise_engaged`, `acc_main_on`, one still-undecoded bit) instead of being built from scratch, and what `tx_hook` value-bounds check is correct for a message that bundles buttons with other live state (Honda's `0x296` is pure button state, so its check doesn't port cleanly). See `research/phase3_tx_allowlist_draft.patch` + `.md`. **MATERIALLY REVISED (2026-08-06): the firmware wall is a *soft* blocker, not a hard one — see `research/panda_safety_firmware_deployability.md`.** Verified against the exact `release-mici` export this device forked from: there is **no signing wall** (the panda bootstub accepts a debug RSA key whose private half is committed in the panda repo, gated on `ALLOW_DEBUG`, which any non-`RELEASE` build sets); **this car already runs debug-signed, non-comma firmware out of necessity** (`panda/board/obj/version` reads `DEV-unknown-DEBUG`, and `SAFETY_SUBARU_PREGLOBAL` is registered only inside `#ifdef ALLOW_DEBUG` — comma's release firmware literally cannot run this car); the firmware ships as a **git-tracked binary** on the release branch (same force-added pattern as the `prebuilt` marker); and `pandad` **reflashes on every boot when the signature differs**, over SPI, with software-only DFU recovery. **⇒ modified safety firmware reaches this device via `git pull` + reboot — no reinstall, no factory reset, no recalibration, no torque relearn.** Build is off-device (needs `arm-none-eabi-gcc` + scons + `pycryptodome`); `panda/SConstruct` does exist in this tree and `gcc-arm-none-eabi` is a core dep, so an on-device build may even be possible — one `which arm-none-eabi-gcc` would settle it. Also found: sunnypilot's preglobal allowlist **already** extends beyond `0x161`/`0x164` (`Throttle` `0x140` + `Brake_Pedal` `0xD1` on the **camera** bus, runtime-selected by a safety-param word sunnypilot added to the panda protocol) — but **no `ES_Brake`/`ES_Status`**, their `tx_hook` value checks for those two are commented out, and `test_subaru_preglobal.py:14` still declares only `[[0x161,0],[0x164,0]]` (zero test coverage for their own additions). |
| Q6 | Does EyeSight accept injected vs physical button? | Phase 3 | **CLOSED (2026-07-23): YES — confirmed on real telemetry, first successful actuation of this whole project.** Full path: passive archive correlation (592 real presses, 98-99.5% clean, see above) → parked RX-only recheck found real presses don't activate anything while stationary (would have made a parked commanded-value test unreadable, so that version was never deployed) → redesigned as an autonomous driving test (v2, no SSH/WiFi needed during the drive — armed before leaving, gated on sustained `vEgo>25mph`+not-already-engaged, analyzed afterward from the auto-synced rlog archive) → **deployed and fired for real**, 23:10:42.888 UTC at 28.9mph, `real_cruise_button=0` (purely software-commanded). Pulled the synced route and confirmed on actual telemetry, not just the local log file: the commanded `Cruise_Button=2` frame is visible on the wire, and within ~100ms `carState.cruiseState.enabled` flips `False`→`True`, `ES_DashStatus.Cruise_Activated_Dash` flips `False`→`True`, and `Cruise_Set_Speed`/`cruiseState.speed` populate to a real value (**28.006mph, matching the car's actual speed at that instant almost exactly**) — held steady for the full 15s observed afterward, no fault, no anomaly. This is a genuinely different result from the parked check: a real ECU-level engage, triggered purely by software, at driving speed — not just a passive-relay confirmation. One honest caveat: the reaction chain's internal timing is tight and consistent (~100ms end-to-end) but sits ~0.8-0.9s before the test hook's own logged timestamp, a real GPS-anchor/wall-clock offset in the analysis, not uncertainty about which event was observed (the exact speed-value match rules that out). **Test code has been reverted off the device** (commit `2ba5623`, redeployed, rebooted) — not left running. Still open, deliberately not tested: `3`/`4`/`5` (deep set, resume shallow/deep), sustained/repeated commanded presses vs. a single tap, and the obvious next question this result raises — closing the loop (deciding *when* to command a press) is real Phase-3 actuation territory now, not hypothetical. Full writeup: `research/es_distance_live_test_protocol.md` (RESULT section) + `research/es_distance_cruise_button_finding.md`. **Follow-up round drafted (2026-07-23), not deployed:** RESUME, deep-SET, and a 3× sustained-press burst test, each with its own precondition gate — see `research/es_distance_live_test_protocol_v3.md`. RESUME's gate deliberately requires a *real* engagement (not a commanded one) to have happened first, since real RESUME recalls a stored target rather than capturing current speed like SET does — not assumed to work by analogy to tonight's result. |
| Q7 | GPS/map-match quality on back roads (advance-warning timing)? | Phase 1 | **CLOSED, root cause found** — not a back-road-specific issue at all. `mapd` output zero curvature data for the ENTIRE day (756,678 samples, back roads + highway + city, up to 86.8mph, 2026-07-21) because the on-device OSM region data only covers 24-34°N (Gulf Coast) — the car operates at ~40°N (Ohio), completely outside it. `progress.md`'s original "full CONUS, 5.4GB" claim was stale/wrong (actual: 2.0GB, 6 southern states). Fix in progress: all-state sunnylink map re-download initiated; pending re-verification once complete. |
| Q8 | Does Subaru expose UDS actuation-test commands usable for ACC button emulation? | Phase 2 | **PARTIALLY ANSWERED, verdict: real but doesn't transfer directly** — `opendbc/car/subaru/interface.py` already uses UDS `CommunicationControl` (service `0x28`, `DISABLE_RX_DISABLE_TX`) to silence the EyeSight camera ECU — but only wired up for **global-gen2** cars with full long control, not preglobal, and not for the cruise-button message. Separately, a real community effort (`github.com/jglim/UnlockECU`, verified directly) is reverse-engineering Subaru SSM4's seed-key security-access algorithm — but for a **2020-2022 Legacy/Outback module (global platform)**, not a preglobal car like this one. May not transfer without independent work on the actual preglobal EyeSight module's security access. |
| Q9 | Does preglobal's cruise/EyeSight module respond to a UDS `CommunicationControl`-style "stop transmitting" request the way global-gen2's does? | Phase 2 | **CLOSED (2026-07-23, real on-device test) — NO.** The EyeSight ECU accepts `DiagnosticSessionControl` (extended session) but never responded at all to `CommunicationControl` (`0x28`), and directly-measured `CruiseControl` (0x144) traffic never stopped transmitting during the attempt. The global-gen2 software silence trick has no working path on this preglobal car. **Round 3 (same day) retested from 3 more angles — other bus, opendbc's exact production retry logic, and a SecurityAccess probe — all came back negative too**, upgrading this from "one failed attempt" to a thorough multi-method result. Bus-contention has no known software workaround as a result — see the 2026-07-23 changelog entries below for the full test writeup and what this means for Phase 3. **Camera-bus injection follow-up (2026-07-23, real on-device test) — ALSO CLOSED, and more decisively than the CAN-error-counter check alone suggested.** Sent a single unmodified copy of a real `CruiseControl` frame onto the camera bus (bus 2), reasoning that Throttle/Brake_Pedal's stop-and-go spoof works there without contention. CAN-level error counters stayed completely flat — genuinely looked clean by that measure. **But the car's dash showed a real "EyeSight Off" fault immediately after**, cleared only by a full ignition cycle. The initial "no effect" verdict reported in the moment was wrong and got corrected live once the physical symptom was reported — a real lesson in not trusting a narrow software health check over the actual physical outcome (same category of gap as the plannerd/process-health lesson elsewhere in this project, just at the vehicle-system level instead of the software-process level). **Actual conclusion: something upstream of the CAN-error layer (almost certainly EyeSight itself) is actively validating message consistency/sequencing and faults itself off protectively on a single anomalous frame — not a passive relay.** A sustained injection attempt (which real button-spoofing would require) would mean repeatedly re-triggering this same fault for the entire duration — a materially worse failure mode than the main-bus collision problem, not a workaround for it. This closes the camera-bus route as thoroughly as Q9's original UDS approach. |
| Q11 | Which CAN bit is the physical MAIN/power ACC on/off button (the steering-wheel button that currently does nothing useful since MADS is set to always-on — just a Mario sound + one screen flash on both dash and comma)? | Phase 2/3 | **Bit position CLOSED (2026-07-24) — `CruiseControl` (0x144), byte0, bit2**, matches opendbc's own DBC `OnOffButton` label. **Wiring attempt tried and REVERTED same day (2026-07-24) — see changelog.** Root cause of "always on" confirmed correct (preglobal's `update_mads()` never populated `buttonEvents`), and a debounced fix worked cleanly under slow/deliberate presses, but the car's own `Cruise_On` signal (same message, bit48) turned out to be far less stable under rapid/repeated real presses than the original passive recon suggested (extended false-reading stretches, not just a clean bounce), which raced with the pre-existing `MadsMainCruiseAllowed` auto-enable path and eventually affected the real SET button too. Fully reverted (commit `c619a7b`, params restored) before it could cause an actual drive problem. **User explicitly shelved this ("forget this whole mads on thing") — not scoped for a next session unless he reopens it.** If ever revisited: needs bench-style (not live-driving) iteration given how much rougher `Cruise_On` behaves under real mashing vs. the original careful/spaced test presses. |
| Q12 | Recurring (per user) Qualcomm ADSP audio-DSP failure + camera-sensor shutdown fault causing openpilot to fail to bring up lateral assist (dashcam-mode only — not a vehicle steering safety issue, this car's steering is stock/mechanical) on some ignition cycles. Root cause? | Device reliability | **OPEN (surfaced 2026-07-26).** Root-caused to kernel level, not Python: a burst of `q6asm_callback`/`ADSP_EFAILED` audio-DSP failures followed by a `cam_sensor_shutdown` camera fault, no corresponding `/data/community/crashes/` entry. **Ruled out, with mechanism-level evidence, not just timing**: the new Phase 3 status-dots/alert code (status file written continuously and correctly through the incident, override latch never tripped, nothing armed) and the user's own hypothesis that toggling on `RecordFront`/`RecordAudio` caused it (both toggles gate disk logging only in `loggerd`, not audio/camera hardware init — `micd.py`'s mic stream and the driver camera both init unconditionally every boot regardless). `micd.py` already wraps its stream setup in a 10-attempt retry anticipating exactly this class of ADSP failure, suggesting known/generally-self-mitigated SoC flakiness. Genuinely unresolved: the actual trigger, and whether it's truly periodic as the user believes — no prior mention of `q6asm`/`ADSP`/`cam_sensor` anywhere in this doc's history before today, and `dmesg` only covers the current boot so there's no way to check further back without a recurrence. |
| Q13 | Did the Phase 3 status-dots/one-shot-alert UI feature (commit `029d876b84`) cause the repeated real `commIssue`/TAKE CONTROL disengages on the 2026-07-26 evening 3-hour highway drive, and by what mechanism? | Phase 3 UI (reverted) | **PRACTICALLY RESOLVED (revert stands), mechanism NOT fully resolved — see the 2026-07-26 (evening) changelog entry for the full evidence chain.** Two-pass investigation (7 forks total across both passes) converged on: **~90% confidence the code caused it** — direct (not inferred) message-level correlation across all three real episodes (6/7 invalid-message clusters exactly match the real commIssue timestamps, zero elsewhere), a clean A/B outcome split (zero incidents across 5.5hrs/9 sessions on the reverted code including a directly-comparable 3hr continuous drive, vs. three episodes in ~3hrs on the Phase3 commit, with device free-space explicitly ruled out as the differentiator since it's nearly identical in both datasets), and an architectural fact that this was the only per-frame raw-file I/O anywhere in the UI layer and the only continuous write inside plannerd's real-time loop anywhere in the whole codebase. **But ~30-35% confidence in the originally-proposed specific mechanism** (blocking file write → stale plannerd inputs → invalid flag) — real numbers contradict the simple version: no publish-timing gap anywhere near the real 100ms alive-threshold in either dataset (not the 500ms first assumed), `carState`/`controlsState` stayed clean through every episode, a from-scratch `FrequencyTracker` replication showed `freq_ok=True` at every invalid moment, and there's a logical problem (the freshness check for a cycle is cached before that same cycle's write runs). **Conclusion: don't re-attempt this feature with just a lower write frequency** — the real mechanism was never pinned down, so a frequency tweak isn't a confirmed fix. Any future version needs real cereal/capnp pub-sub (this codebase's universal convention for every other live UI element, no exceptions found), not raw file I/O from inside a control loop. |
| Q14 | Does the preglobal engine ECU actually **obey** `ES_Distance`'s `Cruise_Throttle` field (and does preglobal's `ES_Brake`/`ES_Status` carry real brake/RPM command authority), i.e. is there a continuous longitudinal command channel this project already transmits but has never written? | Phase 4 (real longitudinal) | **OPEN — Stage 0 passive archive analysis run 2026-08-07 (see addendum at the end of this cell); still genuinely open, not confirmed, not killed. Originally surfaced 2026-08-06 — the most promising unexplored lead since Q6, and structurally the same kind of find.** Every actuation this project has done writes exactly one field (`Cruise_Button`) of `ES_Distance` (`0x161`). Verified directly against both upstream `commaai/opendbc` **and sunnypilot's own fork** (the stack actually running on this car): (1) the same message carries `Cruise_Throttle` (bits `0|12`), and `create_preglobal_es_distance()` already rebuilds and **re-checksums the whole frame at 20Hz**, copying `Cruise_Throttle` verbatim from the camera — writing it is a one-line change to an already-shipping code path, no new message/address/rate/counter; (2) `0x161` is in `SUBARU_PG_COMMON_TX_MSGS` with `.check_relay = true`, which per `safety/safety.h:263-285` **blocks camera→main forwarding** of that address, so openpilot is the *sole* transmitter — no bus contention by construction, unlike `0x144` (Q5/Q9); (3) `subaru_preglobal_tx_hook()` applies value checks **only** to `ES_LKAS` steering torque — `ES_Distance` passes unconditionally, **so no panda firmware change is needed**, which means this path does *not* hit the prebuilt-branch landmine (`§11`) that has blocked nearly everything else; (4) the relay is *proven* working on this specific car, since `relay_malfunction` blocks all TX and lateral control works every drive; (5) **Q6/Q10 already proved this car acts on openpilot-authored `0x161` frames** — the open question is only whether a *different field* of that same accepted frame is honored. Preglobal's DBC also defines the full Subaru longitudinal trio: `ES_Brake` (`0x160`, `Brake_Pressure`) and `ES_Status` (`0x162`, `Cruise_RPM`) — those two are *not* TX-allowed and would need a safety change. **Git-history provenance (primary source):** `0x161` was originally named `ES_CruiseThrottle`/`Throttle_Cruise` on preglobal by martinl (commit `2bab99fd`, 2021-12-15) — so the `// 0x161 is ES_CruiseThrottle` comment flagged as a "mismatch" in `es_distance_cruise_button_finding.md` is actually the message's *original preglobal name*, not a global borrowing; and jnewb1 (comma.ai) renamed preglobal's `ES_RPM`→`ES_Status`/`Cruise_RPM` and `ES_Brake`'s signals to match global's longitudinal command names in commit `783a8927` (2023-08-29) — in the middle of his own preglobal-longitudinal work. **Not verified: that the engine obeys any of it.** Decisive next step is fully passive and needs no device — the ~105GB archive already contains `Cruise_Throttle` (bus 2) alongside the engine's own `Throttle`/`Throttle_Cruise`/`Throttle_Body` (`0x140`), `Brake_Pressure` (`0x150`), `CVT_Ratio` and `aEgo` on every drive ever recorded; the lead/lag question (command vs. report) settles the hypothesis for free. **Community archaeology (2026-08-06) both CONFIRMED the premise and named the obstacle** — quotes spot-checked verbatim against the raw local Discord export, not taken second-hand. Preglobal, 10/12/2020: *aileron.me:* "So both ES_CruiseThrottle.Throttle_Cruise and ES_RPM are generated by ES and consumed by ECU?" → *bugsyborromeo:* "yes" (same exchange: ES_RPM "controls engine rpm", "ES_RPM's request traces with the transmission message on the main bus"). That is first-hand testimony from the person who reverse-engineered this platform that `0x161`'s throttle field is an EyeSight-generated command consumed by the engine ECU. **But:** *bugsyborromeo:* "not that simple, ES will error if you set it to 0 and it doesn't hear back the same thing from 0x140" — **EyeSight validates its commanded throttle against the engine's echo in `Throttle` (`0x140`) `Throttle_Cruise` and faults on mismatch**, which applies to a byte-faithful relay just as much as to wholesale replacement. Two independent preglobal owners hit this: `bugsyborromeo` (2015 Outback — **real code recovered and independently verified live at `github.com/bugsy924/openpilot` branch `subaru-long`, tip `7d09fc04`**, writing all three ES fields, panda diff TX-allowing `0x160/0x161/0x162/0x164/0x166` *and* blocking them camera→car = full EyeSight replacement; his own verdict "Don't try it, it's broken"), and `aileron.me` (2017 Outback — wrote zeros, got a real transmission check-engine light; later, after allowlisting `0x162`, "Ok it worked" / "So in theory that's half of long control"). martinl, 3/3/2020: "bugsy tried that and eyesight faults when you just try to rewrite throttle and brake values." **Not a kill, for three reasons:** nobody ever tested a *small, brief* deviation (both attempts commanded literal `0` with EyeSight's messages blocked and substitutes built from hardcoded fields); the cross-check's tolerance is measurable from the archive for free (how tightly `0x140.Throttle_Cruise` tracks `0x161.Cruise_Throttle`, and **what the echo does during a driver gas override** — if it follows the pedal, EyeSight already tolerates large mismatches routinely); and the echo itself is writable with no firmware change, since sunnypilot's stop-and-go already TX-allows `Throttle` (`0x140`) on the camera bus via the `SubaruStopAndGo` param — neither prior attempt tried closing that loop. **Revised Stage 0 ordering: measure the `0x161`→`0x140` echo relationship FIRST**, before the command-vs-report question; it decides whether an echo-spoof is a fallback or a day-one requirement. Full writeup + staged plan: `research/eyesight_throttle_channel.md`; archaeology: `research/preglobal_longitudinal_command_archaeology.md`; passive test design + pre-registered kill criteria: `research/es_longitudinal_command_hypothesis.md` + `research/es_longitudinal_command_correlation.py` (written 2026-08-06, **run 2026-08-07 — see the Stage 0 results addendum immediately below the table**). |

### Q14 addendum — Stage 0 results (2026-08-07)

**STAGE 0 RESULTS (2026-08-07) — passive archive analysis run, zero CAN transmission, zero
device access.** `es_longitudinal_command_correlation.py` was executed for the first time
(282 routes, 3,920 segments, 0 errors, ~2.1M `acc_engaged_clean` ticks — every pre-registered
statistical-power gate cleared by 100x+ margins), plus two follow-up scripts written after
seeing the first results (`es_echo_focus.py`, `es_rpm_focus.py`, wide ±1.5s lag grids to
resolve two pairs whose coarse-grid best fits landed on the ±200ms boundary). Full results
and methodology: `research/es_longitudinal_command_results.md`. **Verdict: H1 survives its
own kill test but is not cleanly confirmed either — genuinely still open, now with far
better-characterized envelopes.** Headline findings: (1) **K1 (the exact-copy kill test)
does not fire for `Cruise_Throttle` anywhere** — no powertrain report reproduces it to the
pre-registered threshold, in any of 6 regimes against any of 16 candidate fields; a real bug
was found and fixed in K1's exact-match branch (false-positive on near-constant fields,
confirmed to affect only `esb_brake_pressure`, never `Cruise_Throttle`). (2) The wide-lag
echo-relationship re-test (priority item per the task brief) found the `0x140.Throttle_Cruise`
↔ `0x161.Cruise_Throttle` correlation is **real but moderate everywhere** (R²≈0.45–0.55
across the *entire* ±1.5s lag range tested, never approaching the 0.999 echo threshold), and
critically the curve is a **smooth broad hump with a minimum right at zero lag** — the
signature of two signals sharing a slow common trend, not a tight fast echo or
command-acknowledgment loop. (3) **The driver-gas-override test (T5, the pre-registered
discriminator) came back genuinely AMBIGUOUS** (relative gain 0.567, same-sign 58.8%,
between the CONFIRM and KILL bands) — and hand-inspection of raw events found real
heterogeneity, not noise: near-floor starts show `Cruise_Throttle` jumping in near-lockstep
with the pedal, while other events show it moving opposite to a real physical throttle surge
(EyeSight backing off as the car outpaces its request — the textbook command signature). One
concrete traced event shows `Cruise_Throttle` pinned at floor through an entire gas press
while pedal/`Throttle_Body` saturate, then jumping ~700ms later and **continuing to climb for
another 1.8s after the pedal is released** — a command-like independent-ramp signature, with
`Throttle_Cruise` (the ECU's own report) tracking that climb closely throughout, consistent
with "ECU acknowledging a growing command" rather than "field mirrors the pedal." (4) **The
cleanest, most decisive evidence in the whole pass is not lead/lag at all — it's the transfer
functions.** `Cruise_Throttle → aEgo` is almost perfectly monotonic across its full range
(floor=808 → −0.66 m/s², ~1800 → near-zero, peak~3750 → +0.50 m/s², huge per-bin sample
sizes), and during EyeSight-braking episodes `Cruise_Throttle` collapses to *exactly* 808 at
both the median and the minimum (n=378, arriving ~600ms after brake onset) — a clean,
coordinated three-message confirmation of the global `THROTTLE_ENGINE_BRAKE` anchor. **H1a
verdict: anchors confirm, range doesn't** — 808/1818 land almost exactly on global's
constants, but the real ceiling on this car is ~3750–3800, not global's 3400 (8.5% of
samples exceed 3400 entirely) — any future control law must use a *locally measured*
ceiling. (5) `ES_Brake.Brake_Pressure → aEgo` (H2, not writable today without a firmware
change) is even cleaner: slope ≈ −0.0056 to −0.0058 m/s²/count, within ~4% of global's exact
constant (−3.5/600 = −0.005833) — the single tightest encoding match found anywhere, on a
field nobody has ever written on this car. (6) **`Cruise_RPM` vs `Transmission_Engine`
(H3, the pre-registered "most confident" prediction) reversed under the wide-lag re-test, and
this needs stating explicitly per the pre-registration rules, not smoothed over.** The
coarse grid found R²=0.9889 at its +200ms boundary; the wide ±1.5s follow-up (`es_rpm_focus.py`)
resolved a genuine interior peak at **R²=0.9905, lag=+400ms** — still 0.0085 short of K1's
0.999 threshold, but with a completely different, much more decisive *shape* than the
throttle pair: a single sharp peak (not a broad ambiguous hump), with even the curve's worst
point (R²=0.916 at −1500ms) far tighter than anything the throttle field ever showed. §4.3
predicted this pair would trip K1 in the **echo** direction, confirming a 2019 DBC comment
that called `Cruise_RPM` a "duplicate" of the transmission's own report. **That's half right,
half wrong: the coupling is real and very tight, but the tightest point sits on the *command*
side of zero lag** — restricting to lag≤0 only, the best available fit is R²=0.9848 at lag=0,
measurably below the +400ms command-side peak, so per the pre-registered sign rule (which
exists precisely to avoid scoring the strongest command-side confirmation as a false kill)
this would resolve as "ECU acknowledging a command," not an echo, even if R² had crossed
0.999. `Cruise_RPM` vs `Engine_RPM` cross-checks the same story independently (R²=0.955 at
+250ms). Net: H3 is not confirmed (same G1 ceiling as H1), but the pre-registered "most
likely dead" framing looks wrong — `Cruise_RPM` now reads as the *most* tightly
powertrain-coupled of the three fields, in the command direction, not the least. (7) Unplanned
finding: `SubaruStopAndGo` (or equivalent) is **continuously active** on this device across
the entire archive — fabricated `Throttle`(`0x140`)/`Brake_Pedal`(`0xD1`) frames toward the
camera bus at within a few percent of the *real* main-bus message rate for the same two
addresses, not brief resume bursts as earlier research assumed; corrects the operational
picture in `eyesight_throttle_channel.md`/`panda_safety_firmware_deployability.md`. (8) T9
relay-fidelity check: the premise holds — 98.7% exact `Cruise_Throttle` match main-vs-camera,
counter increments cleanly 99.1% of the time, ~3ms median relay delay; contrary to the design
doc's pre-run speculation (G11), the relay is highly faithful, not lossy. **Net effect on
next steps: archive data has now done everything it structurally can (G1's identifiability
ceiling reached) — it ruled out the cheapest kill and produced strong evidence this is a
real, physically-meaningful, powertrain-coupled signal, but could not cleanly separate
"command" from "an extremely faithful report" the way a clean CONFIRM would have. No live
test is recommended by this result; the next step, if any, is a design review of what a live
protocol would need to additionally establish, not a live write.** |
| Q10 | Does a commanded SET/RESUME *nudge* an already-engaged cruise target by ~1/5mph, or does it re-baseline the target to current speed regardless of starting state? | Phase 3 controller | **CLOSED (2026-07-24) — NUDGES, does not re-baseline. Confirmed three independent ways in one live test drive (v3 protocol), all in agreement:** (1) **Deep-SET**, verified from pulled `rlog.zst` segments (not just the live snapshot, which was muddied by a real disengage): target held 45.00mph, dropped to exactly **40.00mph** within 0.13s of the commanded press while `vEgo` was 45.5mph at that instant — then held steady at 40.00mph for the next ~4s while `vEgo` drifted independently to 43.56mph. Not a snap-to-current-speed; a clean -5mph decrement. (2) **Burst** (3x shallow-SET, ~50ms apart): target went from 45mph to 44mph (net -1mph, not -3 — see burst finding below) while `vEgo` was 47.19mph at the time of the after-read — again below the old target, not equal to current speed. (3) **RESUME**: after a real disengage, recalled the exact previously-held 40mph target, not the current (much lower, ~13.8mph) speed at the moment of resume. See §10 changelog (2026-07-24) for the full drive writeup. **Unblocks `research/phase3_controller_design.md`**, which was explicitly waiting on this. |

---

### 7.3 Discord archive research pass (2026-07-23)

User uploaded a personal export of 13 comma.ai Discord channels (~60MB total — `subaru`, `dev-opendbc`, `dev-openpilot`, `custom-forks`, `fw-mods`, `toyota-security`, `tuning`, `pedal-interceptor`, `general`, `hw-four`, `hw-three-3x`, `car-port-info`, `openpilot-ui`) to `discord-logs/CommaDiscord/`. Catalogued per-file (topic summary, date range, grep keywords) in `discord-logs/INDEX.md` — check that before blind-grepping the corpus in future sessions.

Four parallel research passes run against this corpus + local opendbc source + web:
- **Q9** — see updated row above; still open, findings in `research/q9_findings.md`.
- **Phase 3 TX-allowlist patch** — drafted, not applied; see Q5 follow-up above.
- **Tailscale plan** — reviewed and revised; see §9.
- **Corrected a stale/wrong claim from an earlier research pass:** an earlier button-bit table had labeled `OnOffButton`/`SET_BUTTON`/`RES_BUTTON`/`Button` all "Speculative (Low)... Unverified" — this is now simply wrong. Q4 is doubly confirmed via 412 real archived events plus a live 12/12 real-time test.

---

## 8. Decisions log

- **2026-07-19** — Chosen approach: EyeSight-setpoint button-spoofing ("ride" longitudinal), **not** direct CAN gas/brake (settled dead-end for pre-global).
- **2026-07-19** — MVP = read-only advisory with human-in-the-loop actuator, before any TX.
- **2026-07-19** — Phase-1 delivery via existing alert/event pipeline, not a custom HUD widget.
- **2026-07-20** — Forked `sunnypilot/openpilot` to a personal GitHub account to have somewhere to build Phase 1+; renamed `PoorMansLong` → `sunnypilot` mid-session for compatibility with `install.sunnypilot.ai`'s custom-fork installer.
- **2026-07-20** — Built Phase 1 on a dedicated branch (`phase1-curve-advisory`) off `release-mici`, not directly on `release-mici` — kept it abandonable/reviewable before committing to the daily-driver branch.
- **2026-07-20** — Chose MTSC (map) only as the Phase 1 trigger source, not vision — matches the original "advance warning beyond sensor range" use case; vision (near-field) held back for a possible later cross-check.
- **2026-07-20** — `CurveSpeedAdvisory` ships default-off with no on-device UI toggle for v1 — SSH/params-file only, revisit if that becomes annoying.
- **2026-07-20** — Added a ~35 mph minimum-speed gate on the advisory after recognizing OSM curvature doesn't distinguish a highway curve from a city roundabout — avoids in-town false positives.
- **2026-07-20** — Patched `ui_state.py`'s product-level gate that force-deletes `SmartCruiseControlMap`/`SmartCruiseControlVision` on cars without long control or ICBM — verified this doesn't apply to Phase 1's read-only use case, confirmed the param now persists on-device.
- **2026-07-20** — Confirmed (via sunnypilot's own community forum) that switching git *forks* costs a mandatory factory reset + calibration/torque relearn; switching *branches within the same already-installed fork* does not, since calibration/torque data lives in `/data/params`, untouched by a `git checkout` in `/data/openpilot`.
- **2026-07-23 — New feature direction opened: lead-vehicle closing-speed advisory ("Phase 1.5").** User's actual daily pain point turned out to be broader than curves: highway cruise locked at speed, no lead tracked yet, traffic ahead visibly queuing/slowing, EyeSight doesn't react until uncomfortably close. Confirmed in code that openpilot's own vision-based lead detection (`radarState.leadOne`) runs unconditionally — not gated behind `openpilotLongitudinalControl` anywhere — so this data already exists on every drive, unused, same origin story as MTSC/VTSC before Phase 1. Mined the full local route archive (124 routes/1,375 segments) to check whether it's real: **yes** — see `research/lead_vehicle_warning_analysis.md` for the full writeup, methodology, and raw data (`research/lead_warning_raw_results.json`, reusable script `research/analyze_lead_warning.py`). Headline result: in all 14 real historical episodes where the driver had to physically brake despite cruise being engaged and a lead tracked, the vision model had already detected the closing lead 0.7-31.6s beforehand (median ~7.8s) — the data was there and unused every time. `fcw` (openpilot's own built-in collision-warning flag) never fired once across 245k highway samples — ruled out as a trigger. TTC-at-detection doesn't cleanly separate "will need a brake" from "resolves on its own" (only 14/63 closing-lead episodes actually required braking) — so the proposed algorithm is deliberately a calm MTSC-style advisory, not a severity-tiered alarm the data can't support. **Decision: pursue this as advisory-only**, reusing Phase 1's existing alert pipeline — no CAN changes, no hardware, doesn't touch anything from the Q9/button-injection work. Design is done; not yet implemented. See the analysis doc for the full trigger/debounce pseudocode.

## 9. References

- Repos: `commaai/openpilot`, `commaai/opendbc`, `sunnypilot/sunnypilot`, `martinl` Subaru community work
- **Our fork:** `github.com/khaled-khurram/sunnypilot`, branch `phase1-curve-advisory`
- Features: MTSC / SCC-M, VTSC / SCC-V (`VisionTurnController`), SLC/SLA, `mapd`, ICBM (`IntelligentCruiseButtonManagement`)
- Terms: `ES_LKAS`, `ES_Distance`, `ES_Brake (0x220)`, `SAFETY_SUBARU_PREGLOBAL`, panda safety allowlist, cabana (CAN capture), UDS (diagnostic actuation)
- Community: [ICBM on '17 Impreza thread](https://community.sunnypilot.ai/t/enabling-icbm-on-17-impreza/191) — ICBM confirmed unimplemented for all Subaru, UDS actuation POC mentioned
- Community: [UnlockECU #25](https://github.com/jglim/UnlockECU/issues/25), [UnlockECU #26](https://github.com/jglim/UnlockECU/issues/26) — real, unfinished Subaru SSM4 seed-key reverse-engineering; targets a 2020-2022 (global platform) module, not this preglobal car — verified directly 2026-07-20, see §7.1
- Two early deep-research passes (2026-07-20) mixed real signal with fabricated specifics (invented "verbatim quotes" not present in the actual sources) — every claim from them was independently verified before being used; only the corroborated facts made it into the Q4/Q5/Q8/Q9 rows above and the changelog
- Tooling: Claude Code for the code work; `gh` CLI for pushing from the dev environment; SSH (dedicated key, registered via GitHub) for on-device work; cabana for passive CAN recon (Phase 2)
- **Local route archive (discovered/clarified 2026-07-22):** `<local-rlog-pipeline>` (docker-based, on the NUC) continuously rsyncs raw route data off the comma independent of any active SSH session. Raw per-segment data (full `rlog.zst` with complete CAN, plus `qlog.zst`/camera files) lives at `<local-archive>/comma-routes/raw/<routeId>--<segmentNum>/` — owned `root:<user>`, needs `sudo` to read (passwordless sudo already works for this purpose). ~1225 segments / ~105GB as of 2026-07-22, going back further than this session's own drives. **Means most passive telemetry/CAN analysis on this project no longer needs the comma device reachable at all** — huge for Phase 2 recon specifically. `route-stats` (a sibling service in the same pipeline) already parses these rlogs via `pycapnp` against a standalone `cereal/log.capnp` copy — a working reference if the full openpilot tooling is inconvenient to run on the NUC's x86_64 (vs. the comma's aarch64) architecture.
- **Tailscale: INSTALLED, RUNNING, boot-persistent (2026-07-24).** Goal was SSH access regardless of network (home WiFi vs. phone hotspot) — motivated directly by tonight's Q6 live-test cycle (had to be home on WiFi to arm/pull results). `noexec` on `/data` confirmed **not set** (the open unknown from the 2026-07-23 research below). Root confirmed `ext4,ro` with only 352MB free — too tight to risk the apt/remount-rw path the older research recommended trying first, so went straight for a static `linux/arm64` binary in `/data` instead (binary host moved: `pkg.tailscale.com` is dead, current is `pkgs.tailscale.com`, found by reading Tailscale's own `install.sh`). **Persistence was harder than planned**: `/var` is `tmpfs` on this device, wiped every reboot — root's crontab (the originally planned mechanism) vanished on the very next reboot, never even ran. Real fix: a guarded, backgrounded hook appended to `/data/openpilot/launch_env.sh` (commit `3c1c45a`) — the only thing on this device confirmed to actually survive every reboot, since it's what boots `controlsd`/`plannerd`/everything else too. Needed `sudo -n` in front of `tailscaled` (missed on the first pass, silently failed TUN creation running unprivileged). **Verified end-to-end via a real hands-off reboot**: no SSH/LAN touch during boot, `tailscaled` came back automatically, same IP (`[REDACTED-TAILSCALE-IP]`), SSH over Tailscale worked immediately, direct P2P connection (not just DERP-relayed) confirmed via `tailscale ping`, onroad process health + HEAD unaffected by the `launch_env.sh` change. **Phone-hotspot reachability CONFIRMED (2026-07-24, later same day, live during a drive):** LAN path went fully dead (`No route to host` on `[REDACTED-HOME-LAN-IP]`, confirming the device was truly off home WiFi, not a stale check) while device was tethered to the driver's phone hotspot (ESSID `iPhone`, `wlan0`, `[REDACTED-HOTSPOT-IP]`) — Tailscale still resolved a **direct P2P path**, not DERP-relayed, ~89ms ping, immediate SSH. Used for real during the v3 button test drive (see §7 Q10/button-matrix and §10 changelog): live-tailed the test log over the tailnet the whole drive, and after the drive pulled raw `rlog.zst` segments directly off the device via `scp` over Tailscale — confirmed this doesn't need the NUC's independent rsync sync at all, since the source segments already exist locally on the device the whole time it's driving. **Still not tested**: survival across an actual AGNOS OS update (vs. just a reboot) — inference only, not a direct test. Full writeup + the original research this superseded: `research/tailscale_findings.md`.
- **Discord archive (added 2026-07-23):** user's personal export of 13 comma.ai Discord channels (~60MB) at `discord-logs/CommaDiscord/`, catalogued per-file in `discord-logs/INDEX.md` (topic summary + grep keywords per channel) — check the index before blind-grepping in future sessions.

## 10. Changelog

- **2026-07-19** — Doc created. Vision, constraints, physics, phase plan, difficulty read, open questions captured.
- **2026-07-19** — Q1–Q3 closed: UI confirmed raylib/`MiciMainLayout` (no Qt), `mapd` running with full CONUS OSM (5.4GB, current), EyeSight floor 25 mph with 2-stage ±1/±5 rocker (spam > hold for ramp speed). Phase 0 now down to a single item: the baseline intervention-route drive.
- **2026-07-20** — Phase 1 implemented and deployed: curve-speed advisory built on `curveSpeedAdvisory` event + MTSC's existing speed-target math, shipped via the existing alert/event pipeline with zero new UI widgets. Forked to personal GitHub, switched device tracking to the fork, worked through a factory-reset requirement (fork switches only, not branch switches), discovered and fixed a product-level settings gate (`ui_state.py`) that was silently deleting `SmartCruiseControlMap` on cars without long control or ICBM, added a minimum-speed gate to keep the advisory quiet in cities/roundabouts. Confirmed via sunnypilot's own community forum that ICBM (Phase 3's exact mechanism) is unimplemented for all Subaru platforms, with a new UDS-actuation research lead for Phase 2. Both required params confirmed set on-device; pending the actual validation drive against Phase 1's acceptance criteria.
- **2026-07-20** — Phase 2 recon started early (ahead of Phase 1's validation drive, opportunistically): found candidate button-message decode (`0x144 CruiseControl`, unverified bit positions) directly in opendbc's own DBC — closes most of Q4/Q5. Ran a deep-research LLM pass on the remaining unknowns; report mixed real findings (verified `UnlockECU` project, correct CAN bus-off physics) with fabricated specifics and at least one clearly mismatched citation — see §7.1 for the full verdict log. Found, by reading opendbc's own code rather than external research, that a UDS `CommunicationControl` technique for silencing a competing ECU transmitter already exists and works for global-gen2 Subarus — an unexplored potential alternative to physical bus-cutting for preglobal (new Q9).
- **2026-07-20** — Ran a second, more rigorous deep-research pass. Confirmed one genuinely new fact (SSM3 covers preglobal up to 2015, SSM4 covers 2016+ — independent grounding that `UnlockECU`'s target ECU is off-generation). But caught a second fabrication, this one presented as a verbatim quote on the single most load-bearing claim in the report (preglobal security-access byte length) — see §7.2. Conclusion after two research passes: the remaining open question (exact button bit positions) needs an actual passive CAN capture, not more web research — that's the next real step, not another research prompt.
- **2026-07-21** — **Active incident opened.** First real validation drive on `phase1-curve-advisory` (commit `fa6604a`) resulted in a total cruise-control outage — see §11.
- **2026-07-22** — Speed-limit UI tried (stock `SpeedLimitRenderer` ported to mici, then simplified to a plain orange number after user disliked the first version, then resized/realigned after user disliked the second) — ultimately **removed entirely** (commit `53ac87c`) after the user decided it wasn't worth it even fixed. Kept the alert/HUD z-order fix and the Phase 2 decoder script, both unrelated to speed-limit specifically. Also: a "TAKE CONTROL / Communication Issue Between Processes" alert the user saw on a drive was traced to normal process-startup settling time (`commIssue`, self-resolves within ~10-15s of ignition-on) — confirmed unrelated to any of this session's changes, both by file scope and by timing (the drive in question ran on an older commit than the fix in question).
- **2026-07-22** — Phase 2 button-bit finding (Q4) confirmed a second time, live: built `tools/phase2/live_button_decoder.py`, ran it while the user pressed a real, deliberate button sequence twice (12 presses total). 12/12 correct detections, right direction every time, zero misses/false-positives. Corroborates the archive-mining statistical result from earlier the same day.
- **2026-07-22** — Phase 2 kicked off: mined the local route archive (1225 already-synced segments, no comma-device access needed) for real cruise-button-press events (412 found via `vCruiseCluster` deltas), correlated against raw `0x144` CAN data. Empirically confirmed `SET_BUTTON`=byte0/bit3, `RES_BUTTON`=byte0/bit4 — and corrected an earlier "bits 48/49" claim from a deep-research pass that turned out to be wrong (those are state flags, not button signals). See Q4.
- **2026-07-21** — Root-caused via actual on-device crash logs (not just hypothesis): `common.params_pyx.UnknownKeyName` on the new `CurveSpeedAdvisory` key, because this branch is a prebuilt-only release export with no on-device build system at all (confirmed no `SConstruct` exists on any mici-targeting sunnypilot branch). Shipped a workaround (commit `70051d5`) using an existing codebase pattern (`try/except UnknownKeyName`, default `True`) instead of a full reinstall (which a full wipe/recalibration, confirmed via community chat logs, would have cost). Also found and fixed `SmartCruiseControlMap`/`SmartCruiseControlVision` reverted to `False` on-device. Verified the exact crash line no longer raises, post-reboot, on-device. Remaining gap: real key-on with the car hasn't been observed since the fix — that's the next and last verification step.
- **2026-07-21** — Real drive #1 post-fix: `plannerd` crash confirmed fixed (clean 9-min drive), but curve advisory still didn't fire. Real rlog telemetry showed `map.state` stuck at `disabled` the whole drive — traced to a *second* independent param-wipe gate (`sunnypilot/selfdrive/car/interfaces.py:_cleanup_unsupported_params()`, sibling of the already-patched `ui_state.py` gate). Fixed (commit `d2b3c38`) after a full-tree audit confirmed no third copy exists. Deployed, params re-set, rebooted clean.
- **2026-07-21** — Real drive #2 post-fix, specifically targeting back-road curves: both fixes confirmed holding (`map.state` now correctly toggles disabled/enabled; no crashes), but advisory still didn't fire and the user got a real torque-saturation "take control" moment with zero advance warning. Traced live: `mapd` is outputting zero curvature data for these roads (not a code bug — verified our parsing/wiring is correct, the input itself is empty). Phase 1's code confirmed correct given its inputs; gap is external (`mapd`, third-party vendored binary). Next: highway drive to isolate rural-road-specific vs. device-wide.
- **2026-07-21** — ~2 more hours driving (highway + city, several trips) to test the highway hypothesis. Three parallel forks: process-health audit (100% clean, 10 boot cycles, zero crashes), full-day telemetry parse (all 131 segments — `map.state` toggles correctly but never reaches `turning`, `distance` is `0.0` for all 756,678 samples up to 86.8mph), and a `mapd` black-box deep-dive that found the actual root cause: on-device OSM data only covers 24-34°N, car operates at ~40°N (Ohio) — completely outside the downloaded region. `progress.md`'s original "full CONUS" claim was stale/wrong. User initiated an all-state sunnylink map re-download in response; pending re-verification once it completes. Q7 closed (root cause found, wasn't a back-road-specific issue at all).
- **2026-07-23** — Q6 (`ES_Distance`/`Cruise_Button` live-actuation question) worked end-to-end this session: passive archive correlation (592 real presses, 98-99.5% clean, see above) confirmed the field tracks real presses at scale; a live RX-only recheck (`tools/phase2/live_es_distance_monitor.py`) then found real SET/RES presses don't move `Cruise_Activated_Dash`/`Cruise_Set_Speed` at all while parked/idling (even with `Cruise_On` true via MADS), which would have made a parked commanded-value test unreadable — caught before deploying it, not after. Redesigned as a driving test (v2) that needs no live SSH/WiFi during the drive (device has no Tailscale) — autonomous, gated, one-shot, analyzed afterward from the auto-synced rlog archive.
- **2026-07-24 — v3 live test drive: full button command matrix confirmed, Q10 closed, Tailscale hotspot proven live.** Deployed the previously-drafted, dormant v3 patch (`cb787f3`, three flag-armed one-shot tests: deep-SET/RESUME/burst), armed and ran all three **in a single drive** (not one-per-drive as originally planned — safe to combine once the driver's own real engagement had already legitimately latched RESUME's precondition, so no cross-contamination). First drive to use the newly-installed Tailscale link for live monitoring instead of blind-arm-and-analyze-after: tailed the test log live over the tailnet while tethered to the driver's phone hotspot (confirmed direct P2P, not DERP-relayed, LAN path fully dead — see §9), and after the drive, pulled the raw `rlog.zst` segments straight off the device via `scp` over Tailscale rather than waiting on the NUC's independent sync. **Results, all three fired clean, zero aborts, zero faults:**
  - **Deep-SET** (`cruise_button=3`): target dropped exactly 45→40mph within 0.13s, held steady while `vEgo` drifted independently — closes Q10 (nudge, not re-baseline).
  - **Burst** (3x shallow-SET ~50ms apart — faster than the v3 doc's assumed 250ms spacing, an incorrect cadence assumption in the original comment, now corrected): all 3 sent, none aborted, but net effect was only **-1mph, not -3mph** — the ECU appears to debounce closely-spaced commanded presses into a single logical press, echoing the real steering-wheel rocker's own double-pulse quirk found back in Q4. Real finding for controller rate-limiting design.
  - **RESUME** (`cruise_button=4`): correctly recalled a previously-held 40mph target after a real disengage, not the much-lower current speed at the moment of resume — third independent confirmation of the nudge-not-rebaseline finding.
  - **One real anomaly investigated, not dismissed:** driver reported a brief (1-2s) "TAKE CONTROL IMMEDIATELY" flash after each of the three fires — a real openpilot control-stack alert, not a cosmetic one, so testing paused mid-drive (RESUME held back) until root-caused via a background fork parsing the actual pulled `rlog.zst` segments rather than guessing. Verdict: only the deep-SET window had a matching alert at all (`commIssue`, the same generic process-watchdog blip already characterized 2026-07-22), and its timing lines up with the real driver disengage 2.3s later, not the commanded press 22s earlier. Burst/resume windows: no matching alert found in the pulled telemetry at all. Read: very likely not caused by the test hook, but not a clean 3-for-3 data confirmation either — flagged in §5 Phase 3 status as worth a clean eye if seen again, not treated as fully closed.
  - Test code reverted off the device same session (commit `9b1ad78`), redeployed, rebooted, HEAD + clean-crash-log verified — same discipline as v2.
- **2026-07-24, later same session — MAIN/power ACC button decoded (Q11).** User's actual motivation: MADS is set to always-on, so the physical steering-wheel on/off button currently does nothing (Mario sound + one screen flash on both dash and comma, no functional effect) — wants it wired up as a real fail-safe/off toggle eventually. Found a strong prior directly in opendbc's own DBC (`OnOffButton` at byte0 bit2, same message/byte as the confirmed SET/RES bits) before writing any code — checked the reference first instead of guessing blind. Wrote `tools/phase2/live_main_button_decoder.py` (passive/RX-only, watches all byte0/byte1 bits, not just the hypothesis, same discipline that previously caught a wrong DBC-adjacent claim), deployed it directly via `scp` over Tailscale (no git push/reboot needed — standalone RX-only tool, not part of the shipped build). Hit one real bug first (`ModuleNotFoundError: No module named 'cereal'` — running a script file directly doesn't add the repo root to `sys.path` the way `python3 -c` does; fixed with an explicit `PYTHONPATH=/data/openpilot`) and one noise problem (byte1 bit0's already-known rolling-counter/heartbeat, filtered out). Once fixed: **8/8 clean presses, only byte0 bit2 ever moved, confirmed.** Committed `e86a837`. **Detection only — the button still doesn't do anything.** Wiring it to actually toggle MADS is real control-logic work, explicitly scoped as not-yet-started (see Q11).
- **2026-07-23, later same session** — **v2 deployed and fired for real.** Armed on WiFi, drove normally, fired autonomously at 23:10:42.888 UTC (28.9mph, no real press). Pulled the synced route afterward: commanded `Cruise_Button=2` confirmed on the wire, and the real ECU reacted within ~100ms — `cruiseState.enabled` and `ES_DashStatus.Cruise_Activated_Dash` both flipped true, `Cruise_Set_Speed`/`cruiseState.speed` populated to 28.006mph (matching real `vEgo` almost exactly), held steady 15s, no fault. **First confirmed software-triggered actuation of this entire project.** Q6 closed. Test code reverted off the device same session (commit `2ba5623`) — not left running. Full evidence: `research/es_distance_live_test_protocol.md`.
- **2026-07-23** — Drafted (not deployed) three follow-up ES_Distance live tests: RESUME, deep-SET-while-engaged, and a sustained 3-press burst — each isolates one still-open question from tonight's result (does RESUME's stored-target recall work, does shallow/deep actually differ, can repeated commands be sent safely). See `research/es_distance_live_test_protocol_v3.md` + `research/es_distance_button_test_v3.patch`.
- **2026-07-23** — Drafted (design only, no code) `research/phase3_controller_design.md` for the eventual closed-loop controller — policy, rate limits, driver-override, `plannerd`/`carcontroller.py` architecture split, staged rollout. Surfaced a new load-bearing open question in the process (Q10): tonight's Q6 result only proves SET-shallow engaging from a not-yet-engaged state — whether it *nudges* an already-active target the way the controller concept needs is untested.
- **2026-07-24** — Tailscale installed, running, boot-persistent — see §9 for the full story (binary-in-`/data`, `/var` being `tmpfs` broke the originally-planned cron persistence, real fix was a hook in `launch_env.sh`). Verified via a real hands-off reboot. Motivated directly by tonight's Q6 cycle needing home WiFi to iterate.
- **2026-07-24, new session — MAIN button → MADS wiring attempted, then fully reverted after live testing surfaced a real signal-stability problem.** Root cause confirmed by direct code read: preglobal's `update_mads()` (`opendbc/sunnypilot/car/subaru/mads.py`) only ever populates `buttonEvents` on non-preglobal cars (reads a dash signal that doesn't exist here), so MADS could only ever turn on, never off — exactly matching the user's "always on" complaint. Fix (commit `a5c46f2`): read the already-decoded `OnOffButton` bit directly, debounced (250ms refractory) against its known ~100ms mechanical double-pulse, fed through the same `ButtonType.lkas` path every other brand's dedicated toggle button uses. Deployed via plain `git pull` + reboot (pure Python, no rebuild), process health independently verified (not just trusted a sub-agent's report — real uptime/process-start-time cross-check).
  - **First live round (parked, deliberate spaced presses):** worked, but every press *after* the first double-toggled (disable then auto-re-enable within ~100-300ms), netting to no visible change — looked exactly like "still doesn't work." Root cause, confirmed via a real rlog pull (not guessed): the physical button's press *also* flips the car's own `Cruise_On` bit (same message, same stalk module, same mechanical bounce source), and the **pre-existing** `MadsMainCruiseAllowed` ("Toggle with Main Cruise") mechanism has zero debounce on that signal — it was auto-re-enabling MADS on every `Cruise_On` bounce, undoing the new button toggle. This is very likely also what caused a burst of alerts when the user then buckled up and shifted to Drive mid-flap.
  - **Fix attempt: disabled `MadsMainCruiseAllowed`** (that toggle's own description says it's a fallback specifically "for vehicles without an LFA/LKAS button" — redundant now that a real dedicated button was wired). Params-only change, took effect live via the existing 100ms `read_params()` poll, no reboot needed.
  - **Second live round (rapid real-world button mashing, not the original careful spaced-out test):** worse, not better. Pulled the fresh rlog directly and found `Cruise_On` doesn't just bounce briefly on a press — under repeated real presses it goes unstable for extended stretches (multiple seconds), and since the `ButtonType.lkas` handler in `sunnypilot/mads/mads.py` gates on `(CS.cruiseState.available or self.allow_always)`, presses landing during one of those false-reading stretches get **silently dropped entirely**, not just debounced. Traced 7 consecutive real button presses in a row that produced zero effect this way — this is what "toggled off and won't go back on" actually was. Also traced one case of a single physical press producing two OnOffButton edges ~300ms apart (outside the 250ms debounce window), meaning the debounce window itself wasn't universally sufficient either. The original passive recon (8/8 clean, careful, spaced presses) simply never exercised these harder-mashing conditions, so it looked far more solid than it actually was.
  - **Full revert (commit `c619a7b`, reverts `a5c46f2`) + `MadsMainCruiseAllowed` restored to `True`** — both deployed and reboot-verified (HEAD, param values, process health, zero new crash logs, all independently re-checked, not just trusted from a sub-agent). Car confirmed back to the exact pre-session behavior. **User explicitly shelved the whole idea afterward** ("forget this whole mads on thing") — see updated Q11 row. Session also added (still standing, doc-only, not reverted) a Phase 3 controller-design note in `research/phase3_controller_design.md` §3.5 about needing a persistent master arm/disarm gate, separate from per-event driver override, motivated by the same session's separate discussion about not wanting curve-speed actuation to ever surprise-fire.

---

## 11. 🟡 Incident (opened 2026-07-21, fix deployed 2026-07-21, pending real-drive confirmation)

**Symptom:** On the first real validation drive of `phase1-curve-advisory` (commit `fa6604a`), cruise control was completely unavailable from the start of the drive — repeated attempts to engage both openpilot-assisted cruise and stock/"natural" cruise threw the same persistent error. Car remained driveable manually the whole time (no physical danger, just no ACC). User observed `plannerd` was not running.

**Leading hypothesis (strong circumstantial evidence, not yet confirmed via live traceback):** stale generated capnp C++ bindings. `cereal/custom.capnp` is the schema *source*, but openpilot also ships pre-generated bindings compiled from it (`cereal/gen/cpp/custom.capnp.h` / `.c++`), which require an explicit `scons` build to regenerate — a plain `git pull`/`git checkout` + reboot does **not** rebuild them. Checked both generated files in the local dev clone directly: neither contains the `distance` field or `curveSpeedAdvisory` enum value added on this branch — they're stale relative to the schema source, and nothing (locally or, going by symptoms, possibly on-device) ever regenerated them. `longitudinal_planner.py` unconditionally writes `sccMap.distance = ...` on every single publish call, starting from the first frame — if the compiled schema plannerd actually runs against lacks that field, this throws immediately, matching "immediately... not running" exactly, and a crash-looping `plannerd` plausibly explains losing cruise access entirely (it feeds the core control state machine, not just the advisory).

**Not yet confirmed:** whether comma's manager auto-runs an incremental `scons` build on every boot (if it does, the above theory needs revisiting — the crash could be something else, or the auto-build could itself be failing silently and that failure is the real thing to chase).

**Real process gap this exposed:** earlier "you're clear to test" verification only checked that `SmartCruiseControlMap`/`CurveSpeedAdvisory` params persisted in the params store — never checked that `plannerd` was actually alive and publishing without crashing. Params persisting says nothing about process health. **Do not repeat this gap** — any future redeploy must confirm the actual process is running clean (e.g. `pgrep -af plannerd`, check for crash-loop in logs, tail `plannerd`'s recent log output) before calling it safe to drive on, not just that a param file has the right value in it.

**Root cause, actually confirmed (2026-07-21, via real on-device crash logs at `/data/community/crashes/*.log`, identical on every attempt across the whole failed drive):** the original hypothesis (stale capnp C++ bindings) was **wrong on specifics** — that staleness is real but proven inert (no NativeProcess reads those message types; Python's pycapnp loads schema dynamically, doesn't need the compiled headers). The actual crash:
```
common/params_keys.h added the key "CurveSpeedAdvisory", but the compiled native
extension common/params_pyx.so (which enforces a strict key allowlist) was never
rebuilt, so Params.get_bool("CurveSpeedAdvisory") raises
common.params_pyx.UnknownKeyName on the very first call, in
CurveAdvisoryHelper.__init__, crashing plannerd's constructor on every single
launch attempt.
```
**Deeper structural finding:** this device's branch (`phase1-curve-advisory`, forked from `release-mici`) is a true **prebuilt-only release export** — confirmed no `SConstruct` exists anywhere in the tree, on this branch or on `release-mici` itself, and (checked directly against sunnypilot's GitHub) on *any* mici-targeting branch they ship. Only upstream `master` has a buildable tree, and running raw `master` on a daily driver is explicitly discouraged by the community (unreleased, breaking changes). So **on-device `scons` rebuild is not a viable fix, ever, on this branch lineage** — not a one-time miss, a permanent constraint of shipping on a release/prebuilt branch. The `/data/openpilot/prebuilt` marker file that skips the build step in `launch_chffrplus.sh` is itself git-tracked, shipped by sunnypilot's own release commit — not something we broke.
**Fix actually shipped (2026-07-21, commit `70051d5`):** rather than eat a full reinstall (confirmed via community chat logs: re-running `install.sunnypilot.ai/...` for any branch is a full wipe, same calibration/torque-relearn cost as any other fork/install switch — not a lighter incremental patch), `curve_advisory_helper.py`'s two `get_bool("CurveSpeedAdvisory")` call sites now wrap in `try/except UnknownKeyName`, defaulting to `True`. This mirrors an existing blessed pattern already used elsewhere in this codebase (`settings/toggles.py`, `tools/clip/run.py`) for exactly this "new key, old compiled binary" situation — not an improvised hack. **Tradeoff accepted:** `CurveSpeedAdvisory` isn't really a persisted per-device toggle anymore (its raw param file gets pruned by the params system on every boot since the compiled key list doesn't recognize it — confirmed happening) — it now always defaults enabled until a real reinstall registers the key natively. Revisit if this project ever does a full reinstall for other reasons.
**Bonus finding while fixing:** `SmartCruiseControlMap`/`SmartCruiseControlVision` were both found reading back `False` on-device — contradicting the earlier note in §5 that they were confirmed set. Reset both to `True` via `Params().put_bool()`, confirmed persisted across a reboot. Unclear why/when they reverted; worth keeping an eye on rather than assuming a one-time fluke.
**Verified so far (2026-07-21, device at home, not connected to car):** direct on-device re-exercise of the exact crash line — `CurveAdvisoryHelper()` now constructs standalone without raising, `enabled == True` as intended. Rebooted; no crash-log written; HEAD confirmed at `70051d57`.

**2026-07-21 — Real drive #1 post-fix.** ~9 min back-road drive, 45-58 mph. Result: **`plannerd` crash confirmed fixed** — ran the whole drive clean, no crash logs, normal shutdown at drive end. Lateral control normal. **But the curve advisory never fired**, and this time we had the actual recorded telemetry (rlog) to check rather than guessing: `longitudinalPlanSP.smartCruiseControl.map.state` was `disabled` for **every single frame of the entire drive** — MTSC itself was never active, so the advisory correctly never fired (not an alert-wiring bug). `carControl.enabled` toggled normally throughout, ruling out "cruise wasn't engaged."

**Second bug found and fixed, same day:** `SmartCruiseControlMap`/`SmartCruiseControlVision` were being force-deleted by a *second, independent* enforcement point that nobody had patched — `sunnypilot/selfdrive/car/interfaces.py`'s `_cleanup_unsupported_params()`, called from `card.py` on every boot where the car is connected:
```python
if not CP.openpilotLongitudinalControl and CP_SP.pcmCruiseSpeed:
    params.remove("SmartCruiseControlVision")
    params.remove("SmartCruiseControlMap")
```
For this Subaru, `openpilotLongitudinalControl=False` and `pcmCruiseSpeed` defaults `True` (never overridden — ICBM unavailable for Subaru) → unconditionally true → wipes both params the instant the car actually connects. This is a sibling of the `ui_state.py` gate already patched on 2026-07-20 — same underlying product assumption ("no long control/ICBM → these params don't apply"), enforced in two places independently, only one of which had been fixed. Explains exactly why the param read fine at home but was gone by the time telemetry was recorded.
**Fix (commit `d2b3c38`):** removed the two `params.remove()` calls for this carve-out, same reasoning/style as the existing `ui_state.py` patch. **Full-codebase audit run before patching** (not just the diff) — confirmed these two are the *only* enforcement points of this assumption anywhere in the tree; no third gate exists. Verified against the actual bug on-device (proved unpatched version wipes params, proved patched version doesn't, while correctly leaving genuinely actuation-dependent params like ICBM/`CustomAccIncrementsEnabled` alone). Deployed via plain `git pull` (pure Python, no rebuild needed), params re-set, rebooted clean, no crashes.

**2026-07-21 — Real drive #2 post-fix (back roads again, targeting curves specifically).** Result: **both fixes confirmed holding** — `plannerd` stayed alive, no crashes, and `map.state` correctly toggled `disabled`↔`enabled` in sync with cruise engagement (new behavior vs. drive #1, where it never left `disabled` at all — the param-wipe fix works). **But `state` never once reached `turning`**, and `.distance` was `0.0` for all 8,052 samples across both drives combined — not a threshold/tuning gap, a complete absence of curve data. Pinned the exact moment the user had to take over (a `steerSaturated` torque-limit disengage at 57.7 mph, specific lat/lon logged): `map.state` was `enabled` (armed) at that instant but `distance` was still `0.0` — a curve sharp enough to saturate steering torque, and MTSC had nothing on it.

**Root cause, traced live end-to-end (not inferred):** `MapTargetVelocities` (the mem-param `map_controller.py` reads from `/dev/shm/params`, populated by `mapd`) is a correctly-formatted empty list (`[]`) on-device right now. Our parsing code handles it fine — this is not a wiring/schema bug in anything built or fixed today. **`mapd` itself is producing zero curvature output** for these specific back roads. `mapd` is a vendored third-party ARM binary (`third_party/mapd_pfeiferj/mapd`, pfeiferj's upstream project) with no source in this repo — can't debug further from inside this codebase alone. Given it's a total absence rather than degraded/sparse output, this reads more like a map-matching failure on these road classes than "the curves weren't quite sharp enough" (which would still be Q7, just a softer version of it).

**2026-07-21, later same day — ~2hrs more driving (highway + city, multiple trips) specifically to test the highway hypothesis.** Deployed 3 parallel fork investigations: process-health audit, full-day telemetry parse (all 131 segments), and a direct `mapd` black-box health check. Result, fully conclusive:
- **Process health:** 100% clean across all 10 boot cycles today. Every `plannerd` lifecycle ended in a normal `killing plannerd`/exit-0 (ignition off), zero crashes, zero `UnknownKeyName`. Both fixes holding under real sustained use.
- **Telemetry (all 131 segments, 756,678 samples, speeds up to 86.8 mph):** `map.state` correctly toggles `disabled`↔`enabled` with cruise engagement (40 transitions — confirms the param-wipe fix works) — but `turning` never occurs once, and `distance` is `0.0` for literally every single sample, highway or city, any speed. `curveSpeedAdvisory`: zero fires all day.
- **`mapd` root cause, found directly:** `mapd` itself is healthy (running continuously, actively using CPU, not stuck/crashed). The actual problem: **the on-device OSM region data only covers latitude bands 24-34°N (Gulf Coast/TX/FL)** — confirmed by inspecting `/data/media/0/osm/offline/` directly. The car's live GPS position is **[REDACTED-GPS] (Columbus, OH)** — completely outside the downloaded region. `progress.md`'s earlier claim of "full continental US dataset, 5.4GB" was **wrong/stale** — actual on-disk size was 2.0GB, six southern states only. This fully explains every null result today, back roads or highway: there was never any map data for Ohio to compute curvature from, regardless of road type.
- **Fix deployed and region-verified (2026-07-21, later same night):** user completed an all-state OSM map re-download via sunnylink (`OsmStateName=All`, 390/390 files, confirmed via on-device status params). Verified directly: the exact fine-grained tile covering the car's live position ([REDACTED-GPS-TILE], confirmed to contain the car's real live GPS fix) is now present on disk. **`mapd` needed an explicit restart to pick up the new data** — killing the old process directly did NOT get it auto-relaunched by `manager` (its restart logic only checks whether its own internal process handle is `None`, not real OS-level liveness — a direct `kill` bypasses that check entirely and manager never notices). Had to do a full reboot to get `manager` to launch everything fresh. Confirmed post-reboot: HEAD still `d2b3c38`, no new crashes, `mapd` running with a fresh PID.
  **Not yet confirmed:** `MapTargetVelocities` still reads `[]` while parked — but this was also true before, with the old broken region data, while parked. This feature is about forward-path curvature and may fundamentally require actual movement to compute anything, so an empty result while stationary isn't itself evidence of failure. **The real test is the next drive** — that's the one thing that will actually tell us whether this is fixed.

**2026-07-22 — IT WORKED.** First real city drive after the map fix (routes `00000013--36c63645bb` + `00000014--47507d3d50`, Hilliard/Dublin area). 5 curve detections, **4 fired the advisory correctly** with sensible targets, 0 false positives. One ~100ms geometric flicker didn't fire (looks like noise on a complex stretch, not a bug). One had short lead distance (78ft vs. usual 300-400+) — worth watching if it recurs on tight back-to-back curves.

**Specific calibration question resolved:** a roundabout advisory said "reduce to 22mph in 492ft" — felt too fast for a roundabout at first glance. Pulled the actual OSM way (Riggins Rd roundabout, Hilliard) via Overpass API: **OSM itself tags this roundabout `maxspeed=20mph`**, and the raw geometric radius (~23m, near-perfect circle) computes a hard floor of ~15.2mph. The system's 22mph sits correctly between those — this is a genuinely moderate-radius suburban roundabout, not a tight mini-roundabout requiring single-digit speeds. Verdict: **the math is correct, initial intuition about this specific roundabout was off.** Good validation that `mapd`'s curvature computation (confirmed via its actual Go source: 3-point circumradius, `v_max = sqrt(2.0/curvature)`) tracks real-world posted speeds closely.

**Against Phase 1's acceptance criteria (§5):** small sample (one drive, 5 events) but trending exactly where it needs to be — 4/5 correct, 0 false positives, no code changes needed. **Highway drive (planned 2026-07-23) is the next and likely final data point before declaring Phase 1 done.**

**2026-07-23 — Discord archive research pass.** User uploaded a personal 60MB/13-channel Discord export (`discord-logs/CommaDiscord/`); catalogued in `discord-logs/INDEX.md`. Ran 4 parallel research passes against it plus local source and web: **Q9 stays open but meaningfully de-risked/re-scoped** (see §7 Q9 and §7.3, full writeup `research/q9_findings.md`) — Global's `0x28` UDS-silence trick isn't even reliable within Global itself, and a promising-looking 2023 lead turned out to be Global not preglobal, with a contemporaneous community reply explicitly ruling preglobal out at the time. **Found and corrected a stale/wrong claim from an earlier research pass** (its button-bit table called Q4's now-doubly-confirmed findings "unverified"). **Drafted (not applied) a Phase 3 TX-allowlist patch** adding `0x144` to `SUBARU_PG_COMMON_TX_MSGS`, Honda-`0x296`-precedent style, with two flagged open design questions — see Q5 and `research/phase3_tx_allowlist_draft.{patch,md}`. **Revised the Tailscale plan** (§9) after finding real community precedent — standard `apt` installer now preferred over a hand-rolled static binary; `noexec` on `/data` remains the one open unknown, still needs the device in hand.

**Q9 next-step tool run for real (2026-07-23, live on-device):** `tools/phase2/uds_probe.py` — sends `TesterPresent` + `DiagnosticSessionControl(EXTENDED_DIAGNOSTIC)` + a benign `ReadDataByIdentifier(VIN)` to the EyeSight camera ECU at `0x787` (fwdCamera diag address confirmed via `opendbc/car/subaru/fingerprints.py`'s `SUBARU_OUTBACK_PREGLOBAL` entry), using panda's `elm327` safety mode (same read-only mode openpilot itself uses for FW fingerprinting on this car every boot). Car parked, ignition on, engine off. `pandad` stopped first via `sudo systemctl stop comma`, confirmed down before running; device rebooted afterward, confirmed clean recovery (correct HEAD `53ac87c074`, all normal onroad processes back up including `plannerd`/`controlsd`/`selfdrived`, zero crash logs).

**Real result: `DiagnosticSessionControl(EXTENDED_DIAGNOSTIC)` was ACCEPTED on both bus 0 and bus 2.** This is new, direct empirical evidence — not inference — that the preglobal EyeSight ECU on this exact car does speak UDS and will open an extended diagnostic session. The follow-up `ReadDataByIdentifier(VIN)` got a negative response (`request out of range` — DID not supported), which is a minor/expected detail, not a failure of the session itself. **This meaningfully updates Q9's risk picture**: the "SSM3 probably predates full UDS" hypothesis from §7.2 is now partially contradicted — this ECU accepts at least this much of the UDS handshake.

**Q9 CLOSED (2026-07-23, live on-device test): the software silence trick does NOT work on this car.** Follow-up test (`tools/phase2/uds_silence_test.py`) sent `CommunicationControl(DISABLE_RX_DISABLE_TX)` to the same ECU/address/bus that had just accepted the extended diagnostic session. Result: the ECU never responded at all (timeout, not even a refusal) to that service — and, measured directly, the real `CruiseControl` (0x144) broadcast **never stopped transmitting** across baseline/during/after windows (153/100/75 frames — present throughout, confirming the ECU was never actually silenced, so there was nothing to recover from). The follow-up `ENABLE_RX_ENABLE_TX` resume request also timed out, which is moot since nothing was disabled in the first place. **Final answer: this ECU accepts `DiagnosticSessionControl` but not `CommunicationControl` — the global-gen2 software-silence trick has no software path on this preglobal car.** Bus-contention (Q9's original motivating problem) therefore has no known software workaround; any future Phase 3 attempt would need a physical solution (bus-cut/MITM) instead, which is a materially bigger, higher-risk undertaking than anything attempted so far and not something to move toward casually. Test itself was fully safe: car parked, ignition on, engine off, no fake CAN frames ever sent, device rebooted and confirmed healthy afterward (correct HEAD `53ac87c074`, all processes clean, zero crashes).

**Round 3 tool built and deployed, not yet run:** `tools/phase2/uds_variations_test.py` — three more variations before fully closing the software route: (A) same plain request on bus 2, not just bus 0; (B) opendbc's actual production `disable_ecu()` function verbatim (same bytes Subaru's own `interface.py` uses, plus its built-in 10x retry logic, tried on both buses) rather than a single hand-rolled attempt; (C) a harmless `SecurityAccess` "request seed" probe (no key ever sent back, nothing unlocked) to check whether this ECU gates anything behind an auth handshake at all. Same safety envelope as rounds 1/2 — no fake `0x144` frames, explicit resume attempted in a `finally` block after each disable attempt, self-timeout backstop per the UDS spec regardless. Compiles clean locally and on-device.

**Round 3 run for real (2026-07-23): confirms round 2, from three more angles — still no.** (A) Other bus: session accepted, but the disable request itself got total silence again, same as round 2's bus-0 result. (B) opendbc's actual production `disable_ecu()` function, byte-for-byte and retry-logic-for-retry-logic identical to what Subaru's own `interface.py` calls on working cars, tried on both buses: the function reports success (`True`) each time, but that return value only reflects "we got into an extended session and fired the request" — `disable_ecu()` doesn't itself wait for or verify a response to the actual disable command (`timeout=0` on that specific step, confirmed by reading its source). The real signal is the traffic measurement, done independently: `CruiseControl` (0x144) kept broadcasting the entire time, every attempt, both buses (~50-75 frames per window, never zero) — so despite the "success" return value, nothing was ever actually silenced. (C) `SecurityAccess` request-seed: total silence, no seed, no negative response, nothing — same non-response pattern as `CommunicationControl`, suggesting this service isn't implemented/routed on this ECU either, not just declined.

**This is now a thorough, multi-method, multi-bus negative result, not a single failed attempt.** Q9's answer (no software silence path on this preglobal car) stands confirmed rather than just suspected. Device rebooted and confirmed healthy afterward each time. Nothing further planned on the software route unless new information surfaces (e.g. someone else in the community cracks it) — the only remaining path to Phase 3 is physical (bus-cut/MITM), a materially different order of risk/effort not being pursued at this time.

**New idea surfaced, researched same day: speed-limit display ("cherry on top").** Turns out **it already fully exists in this codebase and needs zero new implementation** — `SpeedLimitMode` is already live-set to `1` (display-only), a dedicated UI widget (`speed_limit.py`) is already composed into the mici onroad layout, and it reads OSM `maxspeed` tags off the same tiles `mapd` already uses (no vision/camera-based sign detection exists anywhere in this tree — that part of the idea isn't a real capability here, but isn't needed since the map-based path already works and is already wired up). No blocking gate found (checked the same `interfaces.py`/`ui_state.py` spots that gated MTSC/VTSC twice already — clean here). **Should just show up on the next drive with the map data now fixed — worth checking the screen before assuming any work is needed.**

**2026-07-23, later — Phase 1.5 shipped, EyeSight-isolation researched and closed, camera-bus injection tested and closed with a real physical incident.** Lead-vehicle closing-speed advisory (`LeadClosingAdvisory`, on by default) and an opt-in Phase-3-validation guidance tool (`LeadClosingTestGuidance`, off by default) both built, deployed (commit `7d20543`), and confirmed healthy on-device — see §8 decisions log for the design/telemetry-validation writeup. Separately investigated whether physically isolating EyeSight (à la the 2021 "Giraffe" DIY board) could sidestep Q9's bus-contention problem: confirmed via the actual hardware author's own words that preglobal's harness has no controllable relay at all ("I just don't have a relay to worry about and run direct") — that architecture is global-platform-only, closing this avenue the same way Q9 did. Then tested camera-bus injection directly (reasoning: the stop-and-go Throttle/Brake spoof works on bus 2 without contention, so maybe CruiseControl would too): sent one unmodified copy of a real `CruiseControl` frame onto bus 2. **CAN-level error counters stayed completely flat — but the car's dash showed a real "EyeSight Off" fault immediately after, cleared only by a full ignition cycle.** The in-the-moment verdict ("no visible collision") was wrong and got corrected live once the user reported the physical symptom — the CAN-error-counter check was blind to a real vehicle-level consequence one layer up. Root cause, best understanding: the copied frame's rolling counter/checksum was already stale by the time it was sent (real message had moved on), and something upstream of the CAN layer — almost certainly EyeSight itself — treated the mismatch as anomalous and faulted itself off protectively, rather than passively accepting it. **This closes the camera-bus route as decisively as Q9's UDS approach** — a sustained injection (which real button-spoofing needs) would mean continuously re-triggering this same fault for as long as it ran, a worse failure mode than the main-bus collision problem it was meant to avoid. **Lesson for this project going forward: CAN-level health checks (error counters, bus-off state) are necessary but not sufficient — a vehicle-level symptom check (dash, actual system availability) is required too, the same category of gap as the plannerd/process-health lesson, just one layer higher up the stack.**

**2026-07-23, later still — "think outside the box" research push: three parallel forks, one major new finding.** After the camera-bus incident, user asked for genuinely new (not-yet-closed) software avenues. (1) NHTSA TSB re-verification: the document an earlier unreliable research pass cited on SSM "Active Test" hiding cruise indicators is real and accurate this time, but covers a different platform generation (2020-22 global) and is a cosmetic dashboard-icon test, not real actuation — dead end, cleanly closed. (2) FreeSSM (real open-source Subaru diagnostic tool) source investigation: found genuine memory-address write commands in its SSM2/3/4 protocol layer, architecturally distinct from both CAN injection and the UDS services already exhausted tonight — real but with real gaps (no known address for this car's actual cruise switches, zero precedent of anyone using it for button simulation). (3) RoutineControl/other-ECU research: RoutineControl itself was a dead end, but surfaced a lead — a code comment (`// 0x161 is ES_CruiseThrottle`) that doesn't match `0x161`'s actual variable name (`MSG_SUBARU_PG_ES_Distance`) — flagged explicitly as unverified Discord-only research. **Directly verified against the real DBC and control code afterward, and it held up completely — see the new Q6 entry above and `research/es_distance_cruise_button_finding.md` for the full writeup.** Bottom line: `ES_Distance` already carries a documented, already-transmitted `Cruise_Button` field with real SET/RESUME shallow/deep semantics, structurally safer than anything tried on `CruiseControl` all night (already-owned main-bus relay, not a competing transmitter) — genuinely the most promising unexplored lead of the entire session, never tested live, suggested first step is a passive telemetry correlation (same zero-risk method that closed Q4), not a live test.

**2026-07-23, later still — Q6's suggested passive correlation actually run, full archive.** Built `research/es_distance_correlation.py`: for each of the 592 real `CruiseControl` SET/RES button-press edges found across the full local archive (136 routes, 1,476 segments, run inside the `comma-pipeline-route-stats` container), recorded `ES_Distance`'s `Cruise_Button` value at that moment plus the real `carState.vCruiseCluster` delta that followed. **Result: the SET/RES → `Cruise_Button` mapping is confirmed at production scale** — 98–99.5% of real presses landed exactly on the DBC-documented values (SET→`2`/`3`, RES→`4`/`5`), staleness 30–60ms (fresh), only 6 stray `0`s out of 592 (likely a route-start artifact). **The shallow(1mph)/deep(5mph) split (`2` vs `3`, `4` vs `5`) is not resolved by this pass** — real `vCruiseCluster` deltas came back dominated by ~5mph for both values in each pair, most likely because the 2.5s lookahead window used for "the resulting cluster-speed change" catches multi-press bursts rather than isolating a single press's effect, not a refutation of the DBC comment. See Q6 (updated) and `research/es_distance_cruise_button_finding.md` for the full breakdown. Next step if this gets picked up again: either a tighter isolated-single-press correlation, or a live real-time test the way Q4's bits got double-confirmed — still no software-commanded value has ever been sent, this was passive-only.

**2026-07-24, overnight — Phase 3 controller brainstormed, designed, and implemented in shadow mode. Committed locally, NOT pushed, NOT deployed.** Same-night follow-on from Q10 closing and the MAIN-button/MADS revert. A long design conversation first locked v1 settings into `research/phase3_controller_design.md` §7: the decel-rate constant (**1.94 mph/s**, reverse-derived from the user's own real test — 60→25mph in 18s on a flat backroad, cross-checked against his own 80mph projections, all internally consistent), a live trigger-distance formula (`distance_needed × 1.25` margin) replacing fixed half/quarter-mile buckets (which were checked against real numbers and found to actually fail for an 80→50mph target at quarter-mile range — a genuine deficit, not just "tight"), auto-downgrade to the largest reachable target when lookahead falls short, a ceiling of "never above the driver's own pre-existing set speed," a per-curve-event command budget (not a flat whole-session cap, which real telemetry — 5 curve detections in one drive, 2026-07-22 — showed would exhaust by the second curve), and two independent arm gates (reactive driver-override latch, proactive `Phase3Armed` deliberate-enable). Also corrected §3.5's own recommendation to reuse the MAIN-button MADS toggle as a fast kill switch — dead, since that work was reverted — in favor of the already-existing brake/steering/gas override latch, which needed a 4th trigger (`gasPressed`) added since a driver flooring the gas to overrule a too-slow auto-set speed wasn't previously covered.

User then reopened the project's **original founding pain point** — "EyeSight brakes way too late" for a closing lead car, not just curves — and authorized an autonomous overnight run scoped strictly to Stage-1 shadow mode (per `claude.md`'s standing TX rule and the fresh MADS-incident lesson): full implementation + research, zero CAN writes, `git push`/device deploy explicitly withheld for morning review.

Three parallel research forks ran first: (1) **code precedent** found this session's own earlier assumption wrong — a lead-vehicle closing-speed advisory (`LeadClosingAdvisoryHelper`, on by default) and an opt-in validation tool (`LeadClosingTestGuidanceHelper`, off by default) were *already shipped* (commits `c4db590`/`7d20543`, see 2026-07-23 entry above) with real tuned constants and a real target formula — the plan pivoted from designing a fresh TTC-based trigger to reusing these instead; (2) **Discord archive research** found someone in the community asked this project's exact question independently 8 days earlier ("Anyone tried spoofing ACC buttons to auto-adjust speed... braking too late"), plus real precedent that button-spam cadence matters mechanically (corroborating the existing 1s rate limit) — see `research/lead_actuation_discord_precedent.md`; (3) **TTC/headway grounding** pinned 5.0s TTC (matching both an NHTSA heavy-vehicle FCW standard and openpilot's own existing FCW horizon independently) plus <2.0s time-headway as a secondary check — see `research/ttc_threshold_grounding.md`.

A dedicated backtest fork then checked the existing `LeadClosingAdvisory` trigger (`vRel<-3.0m/s` sustained 0.5s, no-pedal 3s, 20s debounce) against the 63 already-archived closing-lead episodes: **22% precision** (14/63 needed real braking), recall genuinely unresolved (would need re-mining the full archive, flagged honestly rather than glossed over), no single feature found that would improve it. Verdict: build on it as-is — a shallow, reversible nudge clears a much lower bar than a hard alarm would need to. See `research/lead_closing_trigger_backtest.md`.

**Implementation, entirely scoped to `plannerd`-side files — zero changes to `carcontroller.py` or any car-interface file, this whole session.** New files `phase3_curve_controller.py`, `phase3_lead_controller.py`, `phase3_shared.py` (shared override latch + shadow logger), wired into `longitudinal_planner.py`; a Params-gated alert-wording variant added to `events.py` for when actuation is armed. Everything writes to `/data/phase3_shadow_log.jsonl` only — a path nothing car-interface code reads.

**Two independent verification passes, not just trusting the implementing forks' own reports** — direct code reads plus an actual runtime test harness (stubbing only the native/capnp dependencies unavailable in this sandbox, letting real business logic execute unmocked):
- First pass (curve controller alone): found and fixed the fire-down and restore-up phases sharing one budget pool (a big curve could exhaust it walking the target down, stranding the car below the driver's set speed with no path back until the next curve), and a missing 25mph absolute floor that the design doc itself required but the first implementation pass omitted.
- Second pass (after lead-vehicle actuation was added): found and fixed a complete absence of the design's own `MIN_COMMAND_INTERVAL_S=1.0` rate limit (steps were firing every ~50ms planner frame, not once per second — harmless in shadow mode but would have been a real problem live), and both `CURVE_EVENT_BUDGET`/`LEAD_EPISODE_BUDGET` being set to 10 — a number carried over from an earlier *whole-session* cap discussion without being reconsidered for what a *single event's own delta* needs, which couldn't even cover the design doc's own 20-30mph worked examples. Raised both to 60. Re-verified via a synthetic test: shared-latch propagation (a brake tap on either feature latches both off, same simulated frame, confirmed both call orders), curve fire-down + full restore-to-baseline with the now-separated budgets, and lead trigger/target/restore all converging within each feature's own designed tolerance.

**Current state: three commits on `phase1-curve-advisory`, all local — `4e2adbf`, `0d9708c`, `a9401e8`. Not pushed. Not deployed. Device untouched.** Full settings table, both controllers' designs, the backtest results, and both verification passes are written up in `research/phase3_controller_design.md` §7-§9. Next step is explicitly the user's own — review the diff, then decide on the push, same discipline as every other risk-escalation in this project.

**2026-07-24, morning — user authorized the push and the first-ever live actuation test, both features together, public road.** After being walked through the risk (public road + both features on the very first test = can't isolate which feature caused an issue if something's wrong; lead-vehicle actuation had zero live validation, only a 22%-precision backtest) and choosing to proceed anyway, shipped in stages, each independently verified rather than trusted:

- **Commit `4a0e5b9`: wired real CAN actuation** — self-contained `carcontroller.py` read-and-gate hook (deliberately not importing from openpilot's planning stack into opendbc — zero precedent anywhere in the tree for that layering), flag-file arming (`/data/phase3_curve_armed`, `/data/phase3_lead_armed` — switched from `Params()`, which hits the same compiled-allowlist landmine `CurveSpeedAdvisory` already has on this prebuilt branch), a command arbiter enforcing one write per planner cycle plus a new whole-drive `SESSION_COMMAND_CAP=30` backstop, and `MIN_COMMAND_INTERVAL_S` tightened 1.0→2.0s for the first test specifically. Verified end-to-end against the real committed source before shipping: one-write-per-cycle arbitration, exact session-cap enforcement, and all four override signals correctly overriding even a fresh valid command file.
- **Deploy caught a real crash before any driving happened** (user asked for a process-health check before pulling out — that's what caught it): `plannerd` was down, confirmed via the actual crash traceback, not guessed — `AttributeError: struct has no such member; name = cruise_button`. Root cause: `cruise_button` only exists on opendbc's raw `CarState` object inside `carcontroller.py`'s own scope, not on the capnp-published `sm['carState']` that `plannerd` reads — a false assumption from earlier verification that `CS.cruise_button` would work the same way `CS.gasPressed`/`brakePressed`/`steeringPressed` do (those three are real schema fields, that one isn't). `CS.buttonEvents`, the schema-correct signal, is never populated at all for this preglobal car (same gap the earlier MADS investigation found). **Fix (commit `dba5d57`)**: stop passing `CS.cruise_button` at the `plannerd` level entirely — not a safety regression, since `carcontroller.py`'s own independent, unconditional gate already re-checks the real raw value correctly every cycle from the object that actually has it. Redeployed, verified `plannerd` stable for 200+ seconds with zero errors tagged to the new commit before clearing the drive to proceed.

**First live drive (~30 min): zero speed modulation observed, diagnosed from the shadow log, not guessed.** The override latch tripped in the very first gated-on frame for both features — `"latched-off"` was the first and only thing either controller ever logged, and per its own session-long-no-auto-rearm design, it correctly stayed off for the whole drive, including a lead-closing moment near the end the user specifically noticed (traffic ahead, no EyeSight lock, car not slowing). Most likely cause: gas is often still marginally pressed in the exact same frame a driver hits set/resume — an artifact of engagement timing, not a deliberate override; the shadow log couldn't confirm which signal tripped it, a real gap. **Fix (commit `b2acc3e`)**: `GRACE_PERIOD_S=1.5s` suppresses the override *check* (not arming/gating) for that long after first becoming gated-on, and `Phase3OverrideLatch` now records which signal(s) actually tripped it. Verified against 3 simulated scenarios before shipping (engagement-residue gas → absorbed; genuine sustained brake well after grace period → still latches with correct reason; gas held continuously past the grace window → still latches, not a permanent blind spot). Redeployed, confirmed clean.

**The 29:01 off-ramp incident — investigated fully, not just noted.** User reported: exiting the highway, cruise still at ~80mph, EyeSight had no lock on the car ahead, waited to see if lead-detection would kick in, it didn't, had to hard-brake from ~80 to ~20mph in a couple seconds. A fork reconstructed the actual telemetry: a benign, near-zero-relative-speed lead had been tracked continuously for the prior ~56 seconds (user's "EyeSight had a lock" instinct was right for that stretch) before tracking began flickering on/off (likely the ramp geometry) while closing rate escalated each time caught (-4.57 → -7.35 m/s), while vEgo kept climbing to 82.6mph, cruise oblivious. The real brake produced an actual **~6.4 mph/s deceleration — over 3x EyeSight's own comfort-tuned ~1.94mph/s ceiling.** Honest, quantified verdict: even zero-latency Phase 3 intervention couldn't have helped here — the trigger's own 0.5s sustain filter (needed to avoid firing on exactly this kind of flickering noise) couldn't reasonably have fired before ~1.4s before the actual brake, and EyeSight's own decel ceiling caps what's achievable in 1.4s at ~2.7mph against a closing rate that reached 15-20mph. A hard physics ceiling, not a tuning gap — the user's hard brake was the correct, necessary call.

**Button cadence: quantified as the real bottleneck, then fixed with archive-mined data, not a guess.** At shallow-only 1mph/2.0s, the commanded target descended at 0.5 mph/s — slower than EyeSight's own ~1.94 mph/s physical ceiling, meaning cadence itself (not EyeSight's braking) was limiting response speed for any drop bigger than a couple mph. Archive-mined 87 clean same-magnitude bursts (200ms–several seconds) showing zero debounce collapse or overshoot anywhere in that range — see `research/button_cadence_response_curve.md`. **`MIN_COMMAND_INTERVAL_S` tightened to 0.4s (commit `29748f7`)**, chosen from the well-supported 300-500ms band (not the single 200ms data point). Deep-step (cruise_button 3/5) was considered and explicitly declined: a follow-up isolated-single-press analysis found passively-observed magnitude unreliable (37/38 clean isolated samples showed 5mph regardless of shallow/deep coding, conflicting with Q10's own controlled live spoof of btn=2 giving ~1mph) — real magnitude needs a dedicated live test, same method as Q10, before it's trustworthy. Shipped shallow-only at the tighter cadence instead, which alone already resolves the original bottleneck.

**ICBM roadmap research (4 parallel forks, `research/icbm_roadmap_research.md`)**: confirmed stock openpilot has no equivalent (sunnypilot-only invention, not an industry-standard pattern); official ICBM already ships curve/map-based actuation for supported cars (feature parity with what this project built from scratch) plus Speed Limit Assist as a real, concrete "what we're missing" answer; **Honda's official cadence is 50ms — the exact interval that causes full debounce collapse on this Subaru**, confirming braking/button profiles are genuinely car-specific with no universal number to borrow, and validating tonight's own empirical-only methodology; **corrected an assumption** — no Subaru variant (preglobal or global-gen2) has ICBM working anywhere, and global-gen2's own alternative (disabling EyeSight entirely) has real reported reliability problems this project's "ride EyeSight's own setpoint" architecture avoids by construction; and found even official ICBM implementations (Honda Bosch, Mazda) have been reverted/reshipped for the same *category* of bug hit tonight (engagement/init-moment edge cases) — a structural risk of the whole technique, not a mark against this project's own approach.

**First real drive with actual actuation happening (2026-07-24) — three findings, all diagnosed from the shadow log, not guessed.**

1. **Curve fired correctly, but restored to the wrong speed afterward.** User confirmed the 0.4s cadence felt right (gradual, not abrupt) — but after the curve cleared, the car restored to ~59mph instead of the real ~70mph set speed. Root cause, found directly in the shadow log: `baseline_v_cruise_mph` had been snapshotted as **90.1mph — which is exactly `V_CRUISE_MAX` (145kph) converted to mph** — not the driver's real set speed, at the very first gated-on frame. `v_cruise` hadn't settled to the real value in that exact frame, the same race-condition category as the gasPressed grace-period issue from earlier, just a different piece of state getting caught mid-transition.
2. **Curves 2 and 3 didn't fire at all — but the decision logic wasn't broken.** The shadow log shows curve 3 computing a correct, sensible request (76.97→63.61mph) and logging `"hold-arbiter"` instead of sending it. Cause: `SESSION_COMMAND_CAP=30` (added the night before as a first-live-test-only backstop) got exhausted by curve 1 alone — it needed ~20 individual 1mph corrections because MTSC's own live distance/target estimate kept shifting slightly on approach, and the controller re-corrects on every shift. With no reset mechanism, that silently blocked every subsequent curve and lead event for the rest of the drive.
3. **The drive ended with a real steering-torque override, correctly and permanently latching everything off** (`override_reason: "steering"` in the log) — user had to take real control on a third, larger curve. Not a bug — the safety system worked exactly as designed the moment an actual takeover happened.

**Both real bugs fixed same night (commit `64d42da`), verified against the actual failure data before shipping:**
- `baseline_v_cruise_mph` and `sim_target_mph` now resync to the real, current `v_cruise` after a genuine idle period (`SETTLE_TIME_S=2.0s` with no active intervention and no recently-sent command) instead of a one-time snapshot — gated on time-since-last-command rather than comparing `sim_target` to `v_cruise`, specifically so it self-heals even when both started wrong together (confirmed via a replay of the exact buggy scenario: both correctly settle to 70mph within the settle window even starting from the exact 90.1mph buggy state). Also closes a real, separate gap: neither controller can observe real button presses at all (`CS.buttonEvents` isn't populated on this preglobal car — same gap the MADS investigation found), so this is the only way either will ever notice the driver adjusted their real set speed mid-drive.
- `SESSION_COMMAND_CAP` raised 30→500 (200s of continuous firing before it would trigger — still a real backstop against a genuinely runaway policy bug, not a limit real use bumps into).

**Current state: seven commits on `phase1-curve-advisory` since the shadow-mode work — `4a0e5b9`, `dba5d57`, `b2acc3e`, `29748f7`, `64d42da` — all pushed, all deployed, device confirmed healthy after each. Live-armed (`Phase3Armed`/`Phase3LeadArmed` flag files present) as of this writing.** Next real step: another live drive to confirm the fixes hold, plus a new feature request from tonight's drive — **speed-limit-following** (auto-adjust toward posted limits + a configurable buffer, e.g. +5mph, especially valuable on state highways that drop straight into small-town limits) — user's explicit ask, with one real open design question flagged for discussion rather than assumed: if the driver manually corrects the target back down via real buttons because they don't want the auto-adjustment, should that correction just become the new held ceiling (not a full session-wide kill the way the curve/lead override latch works), since a speed-limit disagreement is a preference correction, not a safety override. Also requested: gate the existing passive speed-limit advisory display behind cruise-engaged state (user only wants it while cruise is on, since that's when they're on roads where it's not just clutter).

- **2026-07-24, design session — speed-limit-following (third Phase 3 feature) designed, not implemented.** `research/phase3_speed_limit_following_design.md` written. Worked through the actual open question (how to tell a "correct my preference" button input apart from a "stop everything" input, given `CS.buttonEvents` is never populated on this preglobal car — same gap Q11/`dba5d57` already found) rather than assuming an answer:
  - **Chosen model**: split by channel *and* context, not by trying to infer intent. Pedals/wheel (`brakePressed`/`steeringPressed`/`gasPressed`) keep the exact current unconditional blanket-kill behavior for all three features (curve, lead, SLF) — real, physical inputs are never routine cruise housekeeping, so no context check is needed or wanted there. Cruise-button-detected corrections become **context-gated**: they still trip the full shared session-long latch (killing curve+lead+SLF, unchanged from today) *if* curve or lead has an active, in-flight transient event underway at that moment — but if both are dormant, the correction routes to SLF as a pinned-target update instead, and the shared latch does **not** trip. This is a real, narrow, explicitly-flagged change to curve/lead's own trigger condition (a stray button press while both are dormant no longer pre-emptively silences *future* curve/lead events later in the same drive) — argued as safe because nothing was actually running for that press to interrupt in the first place.
  - **Detecting a button press at all**: since neither controller sees raw `cruise_button` or `buttonEvents`, "a button was pressed" has to be inferred from an unexplained `cruiseState.speed` delta — generalizes the same mechanism the `64d42da` settle-resync fix already uses. A structural simplification proposed for v1 turned out to have a real bug on review, caught before implementation: it claimed "any observed upward (RESUME-shaped) delta is unambiguously external, since SLF and curve's fire-down phase only ever issue SET" — **but curve's own restore phase legitimately issues RESUME too** (confirmed directly against `phase3_curve_controller.py` and tonight's actual shadow log, which has dozens of real `"decision": "restore"` entries produced exactly this way). Shipping the simplification as originally written would misattribute curve's own normal restore-phase commands as external presses. Needs fixing before implementation: the attribution check has to account for curve's restore activity too, not just fire-down.
  - **Pin scope**: per posted-limit segment, not session-long — a manual correction holds for the rest of the *current* segment, but a genuinely new segment (real, debounced limit change) still gets a fresh, unconstrained recompute. Deliberately rejected session-long persistence: it could let a stale correction suppress a later, genuinely lower, safety-relevant limit (school zone, sharper town drop).
  - **Scope decision, flagged not assumed**: v1 is decrease-only. Auto-raising the target toward a newly-higher limit (e.g. re-entering a highway) is a materially new risk category — the first Phase 3 behavior that would push the target to a number the driver never themselves chose this drive, in the *accelerating* direction, untested even once. Deferred to an explicit v1.1 decision rather than folded in as the "natural" symmetric generalization.
  - Also nailed down: buffer param (`Phase3SlfBufferMph`, default 5), target/debounce logic reusing the existing OSM-`maxspeed`/`mapd` pipeline `speed_limit.py` already reads (not duplicated), explicit non-inheritance of the curve advisory's 35mph min-speed gate (would disable SLF for its own primary highway→town use case), a new isolated `Phase3SlfArmed` gate (same two-gate pattern as curve/lead), and `Phase3CommandArbiter` priority order curve > lead > SLF.
  - Flagged, not resolved: the attribution mechanism (beyond the bug above) is logically derived from already-confirmed facts (Q6 timing, Q10 nudge semantics) but has never been exercised against a real concurrent button-press/command-write race — recommended as the first thing validated in SLF's own shadow-mode stage. Segment-debounce window (~2-3s) is a guess pending its own telemetry pass, same status `MIN_COMMAND_INTERVAL_S` had before cadence archive-mining grounded it. Whether `mapd`'s live-position read is reliable enough for actuation (not just display) grade use is unaudited. Review also flagged that §3's named false-positive case (coincidence with a dormant lead episode) misses a more common one: coincidence with an active curve *restore*, which — per tonight's telemetry — is a longer, far more frequent window than lead episodes are.
  - **§8 (separate, already-decided)**: the existing passive speed-limit display should gate on `cruiseState.enabled` — one-line visibility-condition change, unrelated to the actuation design, no open question.
  - Nothing implemented, nothing pushed to the device — this repo has no code/device access (per `claude.md`); a separate session with the real dev environment integrates and tests it, after the RESUME-attribution bug above is fixed.

**2026-07-26 — Phase 3 status dots + one-shot override alert designed, implemented, deployed, live-verified.** Three peripheral onroad dots (curve/lead/SLF, fixed left-to-right order, `off`/`idle`/`active` states only — no separate visible "latched" state, since a new one-shot alert now carries the *why*) plus an alert that fires once when the shared override latch trips, showing which input (gas/brake/steering, or a combination) caused it — closing the "cruise stays engaged, Phase 3 silently drops out underneath it, zero on-screen feedback" gap this project has lived with since Phase 3 first went live. Design went through a long conversation before any code, including an interactive HTML mockup artifact (size/state/layout tuned live in a browser, no deploy/reboot cycles needed to iterate on look-and-feel) and a formal implementation plan (written, reviewed, approved) before executing. Architecture was ground-truthed via parallel research at every claim rather than assumed: confirmed the onroad UI is Python/raylib (`selfdrive/ui/mici/onroad/`), not Qt; confirmed state transport had to be a brand-new plain `/data/phase3_ui_status.json` file written by plannerd (not `Params()`, not a new capnp field — both dead on this prebuilt branch, no `SConstruct` anywhere, same landmine as every other new-state attempt this project has hit); and confirmed the alert reuses the one already-compiled, already-unused `EventNameSP.leadClosingTestGuidance` slot (its own original trigger, gated on a `Params()` key never added to the compiled allowlist, had never actually fired on this build either — repurposing it removed nothing live) rather than registering a new enum member, which is confirmed impossible here. Commits `a10f8a52f8` (feature) and `029d876b84` (crash fix, below), both pushed and deployed.

**One real bug found via the deploy-and-live-drive cycle itself, not the pre-deploy audit.** `Phase3SlfController.is_active` was assigned deep inside `update()`, past an override-latch early-return path, with no default set in `__init__` — crashed `plannerd` with an `AttributeError` on the very first real drive after deploy. Fixed by giving it the same `self.is_active = False` default this codebase already uses for `self.armed`/`self.was_active` (added defensively to `Phase3CurveController` too, though its own `is_active` was never actually at risk — it's set unconditionally before any branch). A pre-deploy static-audit pass had already run and found nothing; this only ever surfaced from an actual drive — a real limitation of code review alone here, not a process failure. Redeployed, reboot-verified clean.

**Same test session surfaced a separate, still-open hardware-level issue** (see Q12): one ignition cycle came up with openpilot failing to bring up lateral assist entirely — "TAKE CONTROL" flashed ~5s then nothing, dimmed camera, dashcam-mode only. User correctly identified this as loss-of-assist, not a vehicle steering safety event (this Subaru's actual steering is stock/mechanical, independent of openpilot engagement) — cycling the ignition once fixed it. Root-caused via real kernel logs to a burst of Qualcomm `q6asm_callback`/`ADSP_EFAILED` audio-DSP failures immediately followed by a `cam_sensor_shutdown` camera fault — no corresponding entry ever appeared in `/data/community/crashes/` (kernel-level, not a Python exception). Investigated and ruled out, with real mechanism-level evidence rather than just timing: the new Phase 3 code (the status file kept writing correctly straight through the incident, the override latch never tripped, nothing was armed) and the user's own hypothesis that toggling on `RecordFront`/`RecordAudio` right before the session caused it (both toggles only gate `loggerd` disk writes, confirmed by reading `micd.py`/`loggerd.cc`/`loggerd.h` directly). `micd.py`'s own stream setup already wraps in a 10-attempt retry with a comment anticipating exactly this class of ADSP failure — suggests known, usually-self-mitigated SoC/firmware flakiness. Genuinely still open: the actual trigger, and whether this is truly periodic — this doc had zero prior mention of `q6asm`/`ADSP`/`cam_sensor` before this day.

**2026-07-26, evening — Phase 3 status-dots feature caused a real, repeated commIssue/TAKE CONTROL failure on a 3-hour highway drive; fully reverted same night, then root-caused across two investigation passes to a documented confidence level. This is a separate, more serious incident from the same-day Q12 ADSP issue above — different evidence, different (partially still-open) conclusion, do not conflate the two.**

Driving on commit `029d876b84` (the already-crash-fixed version of the status-dots/alert feature from earlier that day), ~3 hours into a continuous highway drive at ~70mph, the driver hit a real "TAKE CONTROL IMMEDIATELY" disengage — not the car's own steering (stock/mechanical, independent of openpilot), but a real, repeated loss of driver-assist control needing active intervention. Pulled over, power-cycled, worked ~4 minutes, then a second episode followed by a ~36-second burst of roughly 30 repeated disengage events. Given the severity, the two Phase 3 UI commits were reverted immediately via `git revert` (not `reset` — preserves history), before any investigation: commits `e17e87a`/`539367e`, new HEAD `539367ee6b` on `phase3-actuation`, pushed, pulled onto the device, rebooted, and reboot-verified clean — matching this project's standing discipline of reverting immediately when a live test degrades and investigating after, not debugging live in a moving car. Incident rlogs (26 segments, rlog-only, no video) were pulled from the device for offline analysis before the revert reboot could touch them.

**First investigation pass (3 parallel research threads — code review, timeline reconstruction, process-health/timing) found strong circumstantial evidence but real ambiguity.** The code-review pass found a real structural candidate: `write_ui_status()` (writes `/data/phase3_ui_status.json`, added by this feature) runs inside `plannerd`'s real-time control loop before `publish()`, and `selfdrived` has an exact watchdog (`commIssue`) that fires when a subscribed service goes stale — a plausible match. The timeline pass (via `cloudlog` inference at this stage, not yet direct message reads) found `longitudinalPlan`/`longitudinalPlanSP` — exactly what `plannerd` publishes, exactly where the new write sits — invalid in every commIssue episode. But the process-health pass found no cumulative CPU/memory/thermal buildup over the 2-hour drive (all flat), and the one publish-timing gap found near the first disengage was only 116ms — small enough to leave real doubt against an assumed (and, as the second pass found, incorrect) 500ms failure threshold. Verdict at this point: "strong circumstantial evidence, not proven."

**User was unsatisfied with that hedge and asked for a much deeper pass, explicitly requesting a confidence score, and supplied the key missing piece: a natural-experiment control dataset.** After the revert, substantially more driving happened (9 separate sessions, ~5.5 hours total, including one continuous 3-hour drive) on the reverted code with zero reported issues. Since the comma device was offline at the time, this data was pulled not from the device but directly from the home NUC's existing local rsync mirror of route data (already continuously syncing independent of device reachability — same mechanism used for Phase 2's original archive mining) — 330 rlog segments, no video, no comma SSH needed at all. Community research was also done into how other custom-fork developers add UI/status features without hitting this failure mode.

**Second investigation pass substantially changed the picture — tightened the correlation, but also disproved the originally-proposed mechanism.**
- **Exact, direct message-level correlation, not inference this time.** Reading the real `valid` flag off all 29,764 `longitudinalPlan`/`longitudinalPlanSP` messages in the incident data: invalid events cluster into exactly 7 tight windows. 6 of 7 match the real commIssue/TAKE CONTROL timestamps exactly; the 7th is an unrelated parking/reverse-gear transition, checked and correctly excluded. Zero invalid events anywhere else in the dataset.
- **The real commIssue threshold, from source** (`selfdrive/selfdrived/selfdrived.py:395-401`): alive threshold is 10x each service's own frequency — **100ms** for `carState`/`controlsState` (100Hz), not the 500ms assumed in the first pass.
- **The originally-proposed mechanism does not check out numerically.** No publish-timing gap anywhere near 100ms during any real failure in either dataset — median gap 50ms, p99 ~65ms, max ~200-235ms, **zero gaps over 500ms in either dataset, including during the three real incident episodes**. `carState`/`controlsState`'s own `valid`/`canValid`/`canTimeout` stayed clean through every episode. A from-scratch `FrequencyTracker` replication (same window sizes, same 0.8-1.2 band) showed `freq_ok=True` at all 446 invalid moments. And there's a structural/logical problem with the simple story: a cycle's freshness check is cached before that same cycle's file write runs, so the write can't retroactively invalidate its own cycle's check — it could plausibly affect the *next* cycle's margin instead, but nothing in the data confirms that either.
- **Clean A/B outcome comparison, confound explicitly checked.** Zero real commIssue events across all 5.5 hours of reverted-code driving (including the 3-hour continuous session, directly comparable in length to the incident's own pre-failure stretch) vs. three confirmed episodes in ~3 hours on the Phase3 commit — both commits confirmed directly from `initData.gitCommit` in the log data itself, not assumed from deploy timing. `freeSpacePercent` (~10% throughout, flagged as a possible confound in the first pass) is nearly identical between the two datasets — a constant baseline condition of the device, not something that varies with which code ran, so it can't by itself explain why only one dataset failed.
- **Architectural finding.** Every other live UI element in this codebase — `driver_state.py`, `confidence_ball.py`, `hud_renderer.py`, every sunnypilot addition included — reads control-loop state exclusively through real cereal/capnp pub-sub, zero exceptions found. `phase3_dots.py`'s raw-file poll and `write_ui_status()`'s raw-file write were the only per-frame file I/O anywhere in the UI layer and the only continuous write inside `plannerd`'s control loop anywhere in the entire codebase — not a style choice, unprecedented in this specific tree.
- **Community corroboration, real but non-specific.** No exact match for "custom status file → commIssue" in the local comma.ai Discord archive, but real precedent of "commIssue/TAKE CONTROL while driving, fixed by a reboot" as a known recurring consequence of custom-fork modifications generally (different fork, different cause, same symptom shape) — supports the general category, doesn't pin the specific mechanism.
- **One narrower, independent corroboration**: the earlier-noted "override detected" fallback (the new one-shot alert's first read of the status file got the wrong reason, the correct one arrived 30ms later) is a race condition in a *different* process (`selfdrived`) touching the *same* file at nearly the same real-world moment — standalone evidence of some I/O irregularity around that file right at these windows, from unrelated code.

**Final verdict, with explicit confidence scores: ~90% confidence the Phase 3 code caused these incidents** (three largely independent lines of evidence — exact message-level correlation, a clean 0-vs-3 outcome split with the free-space confound explicitly ruled out, and the architectural fact that this pattern is unprecedented anywhere else in the codebase). **~30-35% confidence in the specific originally-proposed mechanism** (blocking write → stale inputs → invalid flag) — the detailed timing numbers actively contradict the simple version of that story, and the real mechanism, if this code is genuinely the cause, must be something more subtle than a single measurable stall (affecting the next cycle's margin rather than the same one, an OS-level scheduling interaction invisible to message-level timestamps, or a subtlety in `commIssue`/`commIssueAvgFreq`'s exact trigger logic) — not resolvable from log analysis alone; would need real on-device timing instrumentation or a deliberate repro to close further. **Practical conclusion: don't re-attempt this feature with just a lower write frequency** — the real mechanism was never pinned down. Any future version needs real cereal/capnp pub-sub, matching this codebase's own universal convention, not raw file I/O from inside a control loop.

**2026-08-06 — "Can EyeSight actually be controlled?" research push: a continuous longitudinal command channel found that this project already transmits.** User asked for a genuinely new avenue after Phase 3's button-spoofing hit its structural ceiling (EyeSight's own ~1.94 mph/s decel rate, its reaction latency, no acceleration authority, not end-to-end). Source-verified against upstream `commaai/opendbc` and sunnypilot's own fork rather than inferred: `ES_Distance` (`0x161`) — the message this project already commands `Cruise_Button` on, at 20Hz, as the relay-isolated sole transmitter, with **zero panda value checks** — also carries `Cruise_Throttle`, currently relayed verbatim from the camera. Preglobal's DBC additionally defines `ES_Brake` (`0x160`, `Brake_Pressure`) and `ES_Status` (`0x162`, `Cruise_RPM`), the same trio upstream **global** longitudinal control writes today. See new Q14 above and `research/eyesight_throttle_channel.md` for the full evidence chain, the proposed fast-path/deep-path architecture (throttle veto layered on the existing button controller), the `min(eyesight, ours)` veto-only v1 safety rule that makes unintended acceleration structurally inexpressible, the "only ever replay values EyeSight itself has commanded on this car" self-calibration principle, and the staged validation plan whose Stage 0 is a **fully passive archive analysis needing no device and carrying no risk**.

**Two secondary findings from the same pass, both from sunnypilot's own shipped code:**
- **The panda TX-allowlist wall (Q5) is lower than assumed.** sunnypilot's `subaru_preglobal.h` already ships a *modified* preglobal allowlist (`SUBARU_PG_STOP_AND_GO_TX_MSGS`, param-flag-selected) to end users. Adding `ES_Brake`/`ES_Status` for real friction-brake authority is therefore a change of a kind this fork already makes for this exact platform — a distribution problem, not an impossibility.
- **sunnypilot already feeds EyeSight fabricated inputs on preglobal, in production.** `opendbc/sunnypilot/car/subaru/subarucan_ext.py`'s `create_throttle()`/`create_brake_pedal()` rebuild the engine's `Throttle` (`0x140`) and `Brake_Pedal` (`0xD1`) messages with invented values (`Throttle_Pedal = 5`, `Speed = 1`) and transmit them **to the camera bus** to make EyeSight release standstill. This is the honest signal-domain version of the "trick the cameras" idea, with real precedent — and since `Brake_Pedal` carries `Speed`, biasing EyeSight's perceived speed is a route to its *full* braking authority. Deliberately ranked **below** the throttle channel: those entries are `check_relay = false` (frames interleave with the still-forwarded real ones — contention by design, fine for a 15-frame nudge, not for continuous control), and it corrupts the same world model stock AEB runs on, which the throttle channel leaves completely untouched.

**2026-08-07 — Stage 0 archive analysis executed for the first time: `es_longitudinal_command_correlation.py` (written 2026-08-06, never run) run against the full 282-route/3,920-segment local archive, fully passive, zero CAN transmission, zero device access, per the task's hard rules.** T0 census sanity-checked first on small samples (8, then 20 routes) before committing to the full pass; found and fixed real bugs along the way (a K1 exact-match false-positive on near-constant fields; a second follow-up script that initially produced zero data because the 20Hz tick-clock address was missing from its own decoder table). Wrote two additional follow-up scripts after seeing the first results, `research/es_echo_focus.py` and `research/es_rpm_focus.py`, to re-test two pairs whose coarse ±200ms lag grid landed on the grid boundary with a wide ±1.5s grid instead. Full results, methodology, and all raw numbers: `research/es_longitudinal_command_results.md`; updated Q14 above with the summary. **Bottom line: the hypothesis survives its cheapest kill test (no report field echoes `Cruise_Throttle`) but does not achieve a clean CONFIRM either** — the lead/lag evidence (T3/T4/T5) came back weak and genuinely heterogeneous (illustrated with a concrete traced override event showing command-like independent ramp dynamics), while the transfer-function evidence (`Cruise_Throttle`→`aEgo`, `ES_Brake`→`aEgo`, and the exact 808-during-braking collapse) is very clean and confirms the field is real and physically meaningful, without settling command-vs-report. One pre-registered prediction (`Cruise_RPM` echoing `Transmission_Engine`) reversed under the wide-lag re-test — the coupling is real and the tightest found anywhere in this pass, but on the *command* side of zero lag, not the echo side — reported explicitly per the pre-registration rules rather than smoothed over. Unplanned finding: `SubaruStopAndGo` (or equivalent) is continuously active on this device across the whole archive, not firing in brief bursts as earlier research assumed. **No live test is recommended by this result.** Per the original task's hard rules, Stage 0 completing does not authorize a live write regardless of outcome — the next step, if any, is a design review.

**2026-08-07 (later) — Stage 0 follow-up: gap-closing, stratification, and a new perception-trigger test.** Full writeup: `research/es_stage0_followup_results.md`. Four results, two of them negative: **(1) C5 was never actually evaluated in the first pass, and it is stronger than credited** — across 122,037 samples of real ACC-off driving, `Cruise_Throttle` takes **exactly 4 distinct values, 99.98% of them 808**, i.e. it is inert while the engine's own throttle is certainly varying freely. That falsifies K4 outright and kills the whole "echo of an engine signal" class; it does *not* touch "report of EyeSight's own internal demand," which remains the G1 ambiguity. A previously-unnoticed three-tier structure also emerged (inert when off → partially live when ACC on-but-not-engaged → fully dynamic when engaged), consistent with a controller that is powered and computing before it is authoritative. **(2) The override-event N was inflated ~4.3x** — 283 raw events collapse to **66 independent maneuvers** under 5s-window deduplication; aggregate T5 numbers barely move (rel. gain 0.567→0.609, same-sign 58.8%→60.9%), so the inflation was distributionally benign, but every T5 confidence interval should widen accordingly. **(3) T5's ambiguity is structure, not noise** — stratifying by pre-press operating point, the two ends of the range land in *opposite* pre-registered bands, monotonically: at the floor (n=24) 79.2% same-sign / rel. gain 2.21 (KILL side), mid-range 2600–3100 (n=13) **69.2% opposite-sign** / rel. gain 0.21 (CONFIRM side). The two are *not* symmetric evidence — the floor cluster has an innocent command-side explanation the metric cannot see (a delayed state transition: CT pinned through the whole press, jumping ~700ms later, still climbing after pedal release), whereas a report physically cannot move *down* while the powertrain moves *up*. **⚠ This stratification was NOT pre-registered** (chosen after seeing the data, small per-bin N) and is recorded explicitly as hypothesis-generating only — a falsifiable prediction for a future deliberate capture drive (≥60% opposite-sign from mid-range starts), **not** as evidence for H1. **(4) Two hypotheses died and are recorded rather than dropped:** T4's weak 59% consistency is *not* explained by small-step noise (stratifying by step magnitude shows no monotonic trend: 52%/19%/63%), so C2 stays unrescued; and **the new perception-trigger test came back INCONCLUSIVE**. That test (`research/es_perception_focus.py`) used `Car_Follow`/`Close_Distance` — carried in the *same frame* as `Cruise_Throttle`, so zero relative timing uncertainty — to get a trigger exogenous to the powertrain, on the logic that a report cannot move before the thing it reports on. Yield was excellent (5,583 raw transitions → 814 acquired + 814 lost clean events) and the responses are demonstrably real and physically coherent (lead acquired → CT −302 counts, 18/22 down; lead lost → CT +276, 14/18 up). But against the fast powertrain signals the ordering is a coin flip with a sub-resolution margin (vs `throttle_body`: 60.1% CT-first, median margin +30ms, only 42.6% by ≥50ms — below the ≥50ms floor declared before the run). The dramatic-looking `engine_rpm` result (88% CT-first, +390ms) is a **red herring** — RPM is slow and CVT-decoupled, and `throttle_body` beats it by ~280ms too. **Root cause found in the traces: `Car_Follow` is a thresholded flag on a continuous internal estimate, and `Cruise_Throttle` is already responding ~300ms *before* the flag flips** — so it is not an exogenous impulse, and the detector's baseline window is contaminated by the pre-flip response (which biases *against* finding a CT lead; noted, deliberately **not** applied as a correction). The binding constraint is the 20Hz ES message rate: EyeSight's response and the powertrain's fall within one to two frames of each other. **G1 is not broken; the archive still cannot separate "command" from "faithful report."**

**2026-08-07 — Round-2 pre-registration written (`research/es_stage0_prereg_round2.md`), before either test is run or its data collected.** Round 1's most interesting finding (the T5 operating-point stratification) was chosen after seeing the data, so it can only count as hypothesis-generating; these two tests exist to convert it into something admissible, which requires fixing criteria in advance. **Test A — perception-impulse retest** (archive, zero risk): same `Car_Follow` trigger, but restricted to events where `Cruise_Throttle` is provably flat through the baseline (peak-to-peak ≤30 counts — derived from the existing 40-count onset threshold, so the baseline by construction contains nothing the detector would call an onset), which fixes both round-1 failures at once (restores a genuine impulse, decontaminates the baseline). Lead-*lost* events excluded from the primary endpoint; `engine_rpm_140` excluded as a comparator by pre-registration (documented red herring — it is slow and CVT-decoupled, `throttle_body` beats it by ~280ms too, and it may not be quoted as a headline). Primary endpoint fixed to one number: fraction of events where `Cruise_Throttle`'s onset precedes `throttle_body`'s by ≥50ms. CONFIRM ≥70% **and** median margin ≥75ms; NULL ≤50% **or** median margin ≤25ms; power floor N≥100 or INCONCLUSIVE. The ~25ms quantization bias (which cuts *in favour* of the hypothesis) is acknowledged and explicitly **not** corrected for. Pre-registered expected outcome recorded as **INCONCLUSIVE ~50% / CONFIRM ~25% / NULL ~25%** — the 20Hz ES rate is still the binding constraint and the filter improves impulse quality, not timing resolution. **Test B — deliberate capture drive** (stock, unmodified ACC; nothing armed, modified or transmitted; ordinary driving, logged passively): tests round 1's one falsifiable prediction — that gas overrides begun with `Cruise_Throttle` in 2600–3100 produce ≥60% opposite-sign responses (a signature a powertrain report cannot produce), with ≤40% falsifying it and a built-in control group of ≥20 maneuvers from the floor (~808) predicted to look same-sign. Power floor N≥30 independent maneuvers (≥5s apart, same dedup rule that collapsed round 1's 283 events to 66); target ≥50. **Committed in advance: if Test B is falsified or inconclusive, §3 of the round-1 writeup is discarded as noise and marked as such here** — so the finding cannot survive on the strength of having been interesting. **Both tests restate G1 explicitly: neither can show the ECU obeys the field, so a full CONFIRM on both raises the prior without closing the question and specifically does not authorize a live write.**

**2026-08-07 — Test A executed exactly as pre-registered, no threshold changes.** Full results: `research/es_stage0_round1_testA_results.md`. **Verdict: INCONCLUSIVE.** `frac_ct_first_50 = 61.82%` (need ≥70% for CONFIRM), `median_margin = 80.06ms` (clears the CONFIRM bar of ≥75ms on its own), `n = 116` (clears the power floor of 100, but not by much — the flat-baseline filter rejected ~86% of otherwise-qualifying candidates, close to what a 40-route smoke sample predicted). A genuine near-miss, not a muddy middle: one of CONFIRM's two required conditions passed, the other fell 8 points short — reported as INCONCLUSIVE per the fixed rule, not rounded up. **A real internal inconsistency surfaced and is disclosed rather than picked around:** `throttle_body` (the primary comparator) shows a real, near-CONFIRM lead, but `wheel_torque` — also a fast, direct actuation signal, used specifically as a cross-check — is close to **chance** on the same 116 events (50.91% win rate, 19.97ms median margin, which alone would satisfy the *NULL* condition). Two signals that both measure "the powertrain moving" disagreeing this much on the same event set is itself evidence against a confident CONFIRM, independent of the primary stat's own shortfall. `engine_rpm_140` again looks dramatic (78.3% by the ≥50ms stat) and is again excluded from the primary result per pre-registration (n=23 only, slow, CVT-decoupled, not informative — a documented red herring, not quoted as a finding). One disclosed methodological artifact: the flat-baseline filter shifted `Cruise_Throttle`'s median onset from round 1's ~100ms to **449.6ms** — an expected consequence of selecting for events with no prior drift (gradual closings excluded, only sharper surprises admitted), not a bug, but a less sharp impulse than the design aimed for and a plausible contributor to the muddier result. The lead-lost secondary (n=858, unfiltered, no verdict) shows the same qualitative shape. **G1 stands — this result doesn't move it either way, being inconclusive by construction.** Next: Round 2 (mirror this same design for `Cruise_RPM`, the field with by far the strongest non-perception correlation of the three, and therefore the best remaining odds of a clean result).

**2026-08-07 — Round 2: `Cruise_RPM` perception-impulse test, pre-registered separately (`research/es_stage0_round2_rpm_prereg.md`, committed alone before the script existed) and executed exactly as specified. Full results: `research/es_stage0_round2_rpm_results.md`. Verdict: NULL — clean and well-powered, not a near-miss.** `frac_rpm_first_50 = 45.1%` (need ≤50% for NULL) and `median_margin = 0.0ms` (need ≤25ms) — both independent NULL conditions fired, on N=107 (clears the 100-event floor). `Cruise_RPM`'s median onset (400.7ms after the `Car_Follow` flip) and `Transmission_Engine`'s (375.1ms) are 25ms apart — within a single 20Hz ES tick, i.e. simultaneous. Onset thresholds for `Cruise_RPM` (60 counts) and `Transmission_Engine` (20 counts) were derived from each field's own frame-to-frame jitter over a fresh 15-route sample, not reused from `Cruise_Throttle`'s thresholds. Two independent smoke-sample extrapolations (40 routes → ~63 projected, 100 routes → ~79 projected) both undershot the eventual real N=107 — recorded as a methods note that extrapolating from early-route smoke samples in this archive is unreliable, and the pre-registration's requirement to run to completion for the real number (rather than trust a projection) was the right call. **The important interpretive result: this NULL sits in real tension with the earlier wide-lag correlation finding** (R²=0.9905, sharp interior peak at lag=+400ms, command-direction, reported above as reversing the pre-registered "duplicate" prediction) — that aggregate, population-level correlation over ~2.1M ticks does **not** reproduce as a causal lead on 107 genuine, isolated, exogenously-triggered events. Read honestly, both can be true at once: the aggregate correlation is a real, population-level phenomenon (same "slow common trend" character already documented for `Cruise_Throttle`/`Throttle_Cruise`'s broad hump), while the event-level causal-precedence claim — which is what would actually matter for whether `Cruise_RPM` is a live command — does not hold up. **This is the correction to record: "most tightly correlated" and "leads causally" are different claims for this field, and only the first survives an exogenous-trigger test.** Combined with Round 1's INCONCLUSIVE on `Cruise_Throttle`, the perception-impulse method has now been tried on the two most promising ES fields and produced zero clean CONFIRMs — informative about the limits of what this archive can settle on its own. G1 unmoved. Next: Round 3 (`ES_Brake`/H2 wide-lag + perception-trigger completion, the last untested field in the trio).

**2026-08-07 — Round 3: `ES_Brake.Brake_Pressure` (H2) wide-lag sweep + perception-impulse test, pre-registered separately (`research/es_stage0_round3_brake_prereg.md`, committed alone before the script existed) and executed exactly as specified. Full results: `research/es_stage0_round3_brake_results.md`. Verdict: CONFIRM — the campaign's first clean one, and not a close one.** Completes the symmetric H1/H2/H3 treatment (`esb_brake_pressure` had previously only had T6's onset-latency test). **Step 1 (wide-lag sweep, n=137,047 active-brake ticks, variance-floor-guarded — `es_std=135.47`, confirming a real signal, not the near-constant artifact that produced a false K1 trigger on this same field earlier in the campaign):** the sharpest, most localized correlation peak found anywhere in this campaign — R²=0.978 at lag=+100ms (command direction) against `brake2_right`, R² falling smoothly and symmetrically to ~0.43-0.46 at both ±1500ms extremes (contrast `Cruise_RPM`'s elevated plateau, worst point 0.916, or `Cruise_Throttle`'s ambiguous hump, worst point 0.500 barely below its own peak of 0.549 — this is the first ES-field correlation in the campaign that looks like a genuine localized peak rather than a flat co-trend or wide plateau). **Step 2 (perception-impulse test, onset thresholds and comparator choice reused directly from T6's already-validated calibration rather than re-derived, response window extended to 2.5s and power floor lowered to N≥50, both fixed in the pre-registration with reasoning — `ES_Brake` requires an extra brake-vs-throttle-only decision, and braking-tied cut-ins are structurally rarer than throttle-modulation events): CONFIRM.** `frac_brake_first_50 = 100.0%`, `median_margin = 499.74ms`, n=73 (clears the floor — a 40-route smoke sample had projected only ~28, the *third* time in this campaign smoke extrapolation has undershot the real full-archive count; now a confirmed general property of this pipeline, not a one-off, and grounds enough on its own to always run to completion rather than trust a projection). **Not a fragile result riding on one comparator: all four independent comparators agree unanimously** — `brake2_left` 100%/500ms, `brake2_right` 100%/530ms, `mc_brake_right` 100%/810ms, `mc_brake_left` 100%/910ms, with the master-cylinder pair lagging the wheel-pressure pair by ~300-400ms exactly as T6 already predicted. The opposite failure mode from Round 1, where a disagreeing secondary comparator undercut a near-miss — here, cross-comparator agreement is part of what makes this credible. The secondary (lead-lost, unfiltered, n=848 raw/20 usable) points the same direction more weakly (80% brake-first), supporting rather than contradicting. A representative trace shows `esb_brake_pressure` already at 57-70 (near peak) by 300-500ms while all three physical brake sensors still read zero — textbook. **G1 still formally applies (cannot prove the brake ECU obeys this field vs. EyeSight publishing it while braking some other, unobserved way) but is weaker here than for the other two fields**: the archaeology research identified no alternative CAN-based braking-command channel on this platform the way an independent throttle-following loop is at least plausible for H1 — recorded as the least-hedged CONFIRM in the campaign, not a loophole closed. Practical caveat unchanged: `ES_Brake` is not TX-allowed today and would need a panda firmware change to ever use — this result changes the confidence picture, not the near-term roadmap. Pattern across all three fields is now complete and does not follow a simple rule (`Cruise_RPM` had comparable correlation strength to `ES_Brake` and went the opposite way) — reinforcing Round 2's lesson that population-level correlation and causal ordering on an exogenous trigger are genuinely different questions. Next: Round 4 (cross-ES-field internal lead/lag structure).

**2026-08-07 — Round 4: two follow-up checks on Round 3's `ES_Brake` CONFIRM. Exploratory/descriptive, not pre-registered — same honesty standard as the T5 stratification. Full results: `research/es_stage0_round4_crossfield_results.md`. Net effect: the CONFIRM stands, but neither check independently corroborates it strongly.** **Component 1 — does `ES_Brake`'s huge margin just reflect an easier detection problem (braking is a big, slow, binary state change; throttle is continuous and near-instant)?** Targeted re-extraction (not a fresh archive pass — direct lookup of Round 1's 116 known `route_id`/`t` pairs) of response magnitude, joined to each event's already-computed margin. **Result argues against the confound, on one key comparison:** stratified by `Cruise_Throttle`'s own excursion size, bigger internal moves modestly predict a cleaner win (53%→75%→92% CT-first across terciles) — expected either way. But stratified by the *physical* `Throttle_Body` response size (the direct analog to Brake's actuator size, which is what the confound theory actually needs), the trend runs **backwards**: the largest physical throttle events show the *worst* margin (15ms, 61% CT-first — barely above chance) versus the smallest (125ms, 81%). If bigger physical events simply won races more easily, this should have gone the other way. Both correlations are weak (r=0.13, r=−0.18, n=110), so this is modest evidence, not a rebuttal — but it points the right direction. **Component 2 — do the three ES fields move together internally (one decision → three outputs, which would make `ES_Brake`'s external margin purely a hydraulic-lag story), or independently?** Wide-lag pairwise correlation between the fields themselves (not vs. powertrain), n=102,362 active-brake ticks. `Cruise_Throttle` vs `Cruise_RPM`: essentially flat everywhere, R²≤0.0017 — no relationship at any lag. `Cruise_Throttle` vs `Brake_Pressure`: also weak, R²≤0.032. `Cruise_RPM` vs `Brake_Pressure`: real structure, R²=0.298 and still climbing at the ±1.5s grid boundary (unresolved, not chased further this round). **A check that materially changed the conclusion, done before trusting the near-zero `Cruise_Throttle` correlations: is that independence real, or is `Cruise_Throttle` just not varying in this window?** Directly measured on a 60-route subsample of the same population (n=13,111): **`Cruise_Throttle` is pinned at exactly 808 in 88.96% of active-brake ticks** (consistent with the original run's T6 finding), while `Cruise_RPM` has real variance (stdev 539.5) in the same window. **This means the `Cruise_Throttle` pairs are INCONCLUSIVE, not evidence of independence** — the test had little power to detect a link that might exist, because one variable barely moves. The `Cruise_RPM`/`Brake_Pressure` relationship is on firmer statistical ground but its interpretation stays ambiguous (real internal link vs. both fields reflecting the same slow multi-second braking-deceleration physics — the same "shared slow trend" character already documented for `Cruise_Throttle`/`Throttle_Cruise`'s broad hump in the original run). **Bottom line: Component 1 offers modest support for the CONFIRM; Component 2's direct test of the main competing hypothesis was underpowered by an unrelated fact about `Cruise_Throttle`'s own behavior in that regime, so it neither corroborates nor undermines Round 3.** One genuinely new, clean finding survives regardless of the confound question: `Cruise_Throttle` and `Cruise_RPM` show no meaningful correlation with each other anywhere in a wide lag window, even both active during the same braking episode — their dynamics are decoupled to first order, independent of the command-vs-report question entirely. Next: Round 5 (value-envelope/transfer-curve completion for `Cruise_RPM`/`Brake_Pressure`, plus a grade-correction attempt).

**2026-08-07 — Round 5: `Cruise_RPM` transfer-curve completion, the Round 4 boundary-clip resolved, and a grade-correction attempt. Descriptive/completeness work, not a command-vs-report test. Full results: `research/es_stage0_round5_envelope_completion_results.md`.** **Task 1 — `Cruise_RPM`'s transfer curve, never measured until now:** binned against `aego` over `acc_engaged_clean` (hundreds of thousands of samples in the dominant bins). Same qualitative shape as `Cruise_Throttle`'s curve — floor≈1000 → −0.631 m/s², zero-crossing≈1900-2000 → −0.007 to −0.026, rising to a broad plateau of +0.45 to +0.53 from ≈2600-3300 — confirmed by `wheel_torque` moving in lockstep. **But unlike `Cruise_Throttle`, the landmarks do NOT match global's constants at all**: global's `RPM_INACTIVE=600` vs this car's measured ≈1900-2000 (over 3x higher); global's `RPM_MAX=3600` vs this car's effective ceiling ≈2600-3300 (lower, not higher). Neither anchor transfers — a stronger, more specific version of the self-calibration warning than `Cruise_Throttle` needed, since there even the floor/inactive anchors matched closely. **Task 2 — Round 4's unresolved boundary-clip** (`Cruise_RPM` vs `Brake_Pressure`, R²=0.298 still climbing at ±1.5s): extended to ±3.0s, n=102,358. The curve is now visibly **U-shaped with a genuine interior minimum around lag=+1000ms** (R²=0.191) and still rising at the new ±3000ms boundary on both ends (0.377 / 0.252) — the same never-converging, minimum-near-zero shape already established for `Cruise_Throttle`/`Throttle_Cruise` and read the same way: **not a lag-locked relationship, a shared slow trend across the span of a braking episode.** Closed with a clean negative characterization, not chased further (a wider grid would only demonstrate the same shape more expensively). **Task 3 — grade-correction attempt (G6), tractable and it sharpens an existing result:** per-route baseline `aEgo` offset estimated from samples where `Cruise_Throttle` sits in a narrow band (1818±100) around global's documented zero-acceleration anchor, `acc_engaged_clean` only; 99/282 routes had ≥20 usable samples. Median offset **−0.198 m/s²** (stdev 0.122, range −0.615 to +0.051) — a real, consistent archive-wide downward bias (rolling resistance and/or net downhill lean in this archive, can't distinguish which) **plus real route-to-route variation**, meaning grade is not a fixed constant a controller could assume. **Applying the correction moves the curve's zero-crossing from ≈1900-2000 uncorrected to ≈1850 corrected (CT=1850 → +0.006, essentially exact zero)** — closer to global's 1818 than the uncorrected curve suggested, meaning the original "anchors confirm" framing undersold how well 1818 transfers; most of the discrepancy was grade/rolling-resistance bias, not an encoding mismatch. Honest limitation: this pass tracked bin means, not variance, so the "does correction reduce noise" question is unmeasured — a future pass wanting that would need per-bin variance tracking. **Two permanent limitations closed out, not chased further:** G7 (`CVT_Ratio`/0x149 has zero decoded DBC signals — the RPM-vs-transmission-ratio confound is structurally untestable with this archive) and G9 (brake/RPM transfer-curve coverage near their ceilings is thin by construction, an archive selection effect only a capture drive can fix). **This closes the substantive-rounds portion of the campaign.** Next: Round 6, final synthesis.

**2026-08-07 — Round 6 (final): campaign synthesis, and the archive-exhaustion campaign for Q14 is now closed. Full rollup: `research/es_stage0_campaign_synthesis.md`.** **Headline, stated first because it inverts the original framing: the one field that produced a clean CONFIRM — `ES_Brake.Brake_Pressure` — is the one field that ISN'T TX-allowed and needs a panda firmware change to ever write. The two fields already writable today with zero firmware change — `Cruise_Throttle` (the field this whole investigation was built around, per `eyesight_throttle_channel.md`'s "day one, no firmware change needed" framing) and `Cruise_RPM` — came back INCONCLUSIVE and a clean NULL, respectively, on the one test type (the perception-impulse race) capable of reaching past the G1 identifiability ceiling.** That tension is the single most important fact for deciding what happens next and cuts directly against the practical premise this project started from. Full per-field pattern (K1, wide-lag correlation shape, perception-impulse verdict, transfer-curve/encoding-transfer quality) is tabulated in the synthesis doc — no simple rule ("strongest correlation wins," "weakest loses") predicts it. **What's now well-established and durable regardless of next steps:** `Cruise_Throttle`'s inertness during ACC-off (C5, kills the whole "echo of an engine signal" hypothesis class); the main-bus relay's high fidelity (T9); `ES_Brake`'s encoding transferring almost exactly from global (~4% slope match, on a field nobody has ever written); `Cruise_Throttle`'s anchors transferring and now sharpened by grade correction (zero-crossing ≈1850, essentially exact match to global's 1818) while its range still doesn't (~3750 measured ceiling vs global's 3400); `Cruise_RPM`'s envelope, measured for the first time, matching *none* of global's constants; `SubaruStopAndGo`'s continuous (not bursty) camera-bus activity; and `Cruise_Throttle`/`Cruise_RPM` being decoupled from each other at every lag tested. **What's still genuinely unresolved:** G1 stands for all three fields, including the CONFIRM — even `ES_Brake`'s result only shows the field precedes the physical brake sensors it's compared against, not that the actuation ECU obeys the CAN field itself, though the alternative-mechanism hypothesis that keeps G1 alive is markedly weaker for brake (no alternative CAN-based braking-command channel identified anywhere in this project) than for throttle (a real, unfalsified alternative: an independent ECU speed-following loop with `Cruise_Throttle` as EyeSight's internal telemetry only). Two loose threads both closed with clean negative characterizations (not left open): `Cruise_RPM`/`Brake_Pressure`'s U-shaped, never-converging lag curve (shared slow trend, not a fast link) and Round 4's underpowered `Cruise_Throttle` cross-field check (808-pinning during braking, already known from T6, not new evidence of independence). Two gaps are structural and permanent: G7 (`CVT_Ratio` never decoded — RPM-vs-transmission-ratio confound untestable) and G9 (brake/RPM curve coverage near their ceilings is thin by construction — only a capture drive fixes this). **Methods retrospective, recorded because it's why the verdicts above are trustworthy:** five real bugs found and disclosed rather than silently patched across the campaign (K1's variance-floor gap; the first perception test's baseline-contamination design flaw; a tick-clock decoder bug that silently zeroed an entire run; smoke-sample extrapolation undershooting the true full-archive N in at least three separate instances, now a confirmed general property of this pipeline; a confound check reported negative rather than dropped). **Bottom line: further passive archive mining has hit diminishing returns** — every remaining thread this campaign identified terminated in either a clean negative or a structural data gap that no additional archive analysis can touch. **The two genuinely useful next steps are both physical, and neither authorizes a live write:** Test B, the deliberate capture drive already pre-registered in `research/es_stage0_prereg_round2.md` (testing the ≥60% opposite-sign prediction from mid-range `Cruise_Throttle` overrides, with a rule committed in advance that a falsified/inconclusive result discards the underlying stratification as noise); and a not-yet-pre-registered adaptation of the perception-impulse test for a deliberate drive rather than archive mining, since the method's power throughout this campaign was consistently limited by how rarely a sufficiently clean, isolated, flat-baseline event occurs naturally in ordinary recorded driving (Round 1's filter alone rejected ~86% of otherwise-qualifying candidates). **G1 will still stand after either one.** This is the end of the archive-analysis campaign for Q14; the question is still open, and the next move needs the user in the car.

**2026-08-07 — Combined capture-drive protocol written and committed** (`research/es_capture_drive_protocol.md`), **before any drive has happened** — same pre-registration-before-data discipline as every round this campaign. Covers both physical next steps in one document: **Test B** (the mid-range gas-override drive, ≥30/target≥50 reps at `Cruise_Throttle`∈[2600,3100] plus ≥20 floor-band control reps, decision rule already fixed in `es_stage0_prereg_round2.md`) and **Test C** (new — a capture-drive version of the perception-impulse test, reusing the exact scripts/thresholds/comparators from Rounds 1-2 verbatim on freshly-collected data, targeting `Cruise_Throttle` primary + `Cruise_RPM` secondary; `ES_Brake` not re-tested, already unanimously confirmed and not writable regardless). Test C's rationale: both archive perception tests were power-limited because `Car_Follow` is a threshold on a continuous internal estimate, so most real lead-acquisitions are gradual and get rejected by the flat-baseline filter (~86% rejection rate in Round 1) — a deliberate drive can bias toward genuinely abrupt lead-acquisitions (lane changes into a car ahead, merges, passing-and-returning) that ordinary incidental driving rarely produces. Honest expectation set in advance: Test B can likely hit its floor in one session (driver-initiated on command); Test C's N≥100 floor will likely need multiple accumulated sessions since it depends on traffic actually being there — re-run the existing scripts against the growing archive after each drive rather than expecting one drive to finish it. Pre-registered outcome predictions recorded before driving: Cruise_Throttle's near-miss archive result could plausibly go either CONFIRM or INCONCLUSIVE with more/cleaner data; Cruise_RPM's clean archive NULL is expected to reproduce, and a reversal would be a surprise worth extra scrutiny. Both tests compose safely in one session — analysis-side gating already excludes any event contaminated by incidental overlap. Same G1 caveat restated at the end: neither test, even fully confirmed, authorizes a live write.

**2026-08-07 (evening) — Capture drive #1 done, first real data against the protocol.** Full results: `research/es_capdrive1_results.md`. ~43 min across 4 routes, synced via the normal pipeline. **Nowhere near either power floor yet — expected, not a setback, per the timeline set in the protocol.** Test C (perception-impulse): `Cruise_Throttle` n=4, `Cruise_RPM` n=7, both INCONCLUSIVE for insufficient N (floor is 100) — specific percentages at this N are noise, not reported as directional. Test B (gas-override): only 1 usable event in the 2600-3100 band (floor is 30) — this drive leaned almost entirely Test C, matching what was told in advance. **Real findings despite the small N:** (1) a genuine bug found and fixed before trusting any of it — the Test B script's first version computed deltas before enough future data existed (same class of bug the settle-then-drain pattern elsewhere prevents), silently returning None for every event; (2) raw `Car_Follow` trigger rate was ~4x the archive's average (235 transitions in 43 min) — the "prefer lane changes/merges" instruction demonstrably worked; (3) the dominant rejection reason was "not isolated" (129/235, 55%) — dense traffic, which is where lane-change opportunities are easiest to find, also means nearby cars keep disrupting the isolation window the test needs. **Actionable for the next drive: moderate traffic instead of dense (fewer other cars shuffling lead status nearby), and more dedicated Test B reps specifically** (this drive was almost all Test C). Continue accumulating across future drives and re-running the same scripts against the growing capture-drive route set.

**2026-08-14 — Full-repo privacy audit, and a real correction to the override-latch record.** User requested a privacy sweep across the docs repo *and* the actual deployed fork (`khaled-khurram/sunnypilot`, branch `phase3-actuation` — cloned read-only into this session for direct inspection, first time this session had access to the real device-install branch rather than just this project's own textual description of it). Findings:
- **Docs repo (this one): a precise GPS fix (`40.07°N/-83.16°W`) plus its containing OSM tile bounds were live in `progress.md`'s Q7 entries, directly contradicting `claude.md`'s own "no GPS coordinates from real drives" scrubbing claim** — redacted to `[REDACTED-GPS]`/`[REDACTED-GPS-TILE]`, `claude.md` corrected to document the violation rather than silently re-assert the (now-true) claim. Git history (`2d1e945`'s parent commit, `869cfa8`) still permanently exposes the owner's first name and real Linux username/local-machine paths — not fixed (would need a full history rewrite + force-push, explicitly deferred pending direct authorization, out of scope for this entry). The two previously-"excised" fabricated-research files (`Subaru_CAN_Reverse_Engineering.md`, `Rigorous_Subaru_Preglobal_CAN_and_UDS_Research.md`) were found to have been silently re-added in their original, uncorrected form by a later sync commit (`22526b4`) — directly contradicting `claude.md`'s standing claim they don't exist in the tree — removed again.
- **The actual deployed fork/branch (`phase3-actuation`, diffed against its real base `release-mici` @ `af744c8` — matches this doc's own recorded fork point exactly): clean.** Grepped the full 22-file/1819-line diff for the same PII categories found in the docs repo — zero hits beyond one benign device-local Tailscale path reference. The car-control code itself was never the source of the leak; it was entirely a docs-repo/git-history problem.
- **Override-latch record corrected, precisely, from reading the real diff rather than trusting a description of it.** Commit `33db679` ("Phase 3 override latch: brake-only, not gas+brake+steering", 2026-08-03) is real and exactly as titled, but it's more nuanced than "gas/steering removed" — it only changes `Phase3OverrideLatch` (the persistent, session-long, plannerd-side latch: brake-only now, matches the commit). **Deliberately untouched, confirmed by the same diff:** `_phase3_read_command_if_safe()` in `opendbc_repo/opendbc/car/subaru/carcontroller.py` — the separate, lower-level, per-cycle gate that blocks transmitting a simulated button press in the exact instant gas, brake, *or* steering-torque is active, independent of whether the session latch has tripped. Two-tier design, both layers real: transient pedal/wheel contact pauses actuation for that instant without permanently disarming the session; only brake causes the lasting session-long disarm. `README.md`'s earlier "brake-only" description (from this same conversation, before the fork repo was actually inspected) has been corrected to state both tiers accurately.
- **Install-readiness gap, found while inspecting the same diff for an unrelated question ("is this installable by a random stranger via the install link"):** `CurveSpeedAdvisory` is now a real compiled `Params()` key (`common/params_keys.h`, default `"0"`/off) — the old `UnknownKeyName` crash-workaround landmine is gone for this specific key. `SmartCruiseControlMap`/`SmartCruiseControlVision` now have a working on-device Settings toggle (`selfdrive/ui/sunnypilot/layouts/settings/cruise.py`'s force-wipe-on-disable-only fix, confirmed in the diff) — no SSH needed for the read-only advisory path. **But all three Phase 3 actuation arm gates (`Phase3Armed`/`Phase3LeadArmed`/`Phase3SlfArmed`) are flag-file-only (`/data/phase3_{curve,lead,slf}_armed`, `os.path.exists()`-gated) with zero on-device UI wiring anywhere in the diff** — arming real actuation still requires SSH and manually creating those files by hand. Also: `events.py`'s `_phase3_curve_armed()` reads `Params().get_bool("Phase3Armed")` for alert-wording only (cosmetic, correctly wrapped in `try/except UnknownKeyName`) — but `"Phase3Armed"` itself was never added to the compiled allowlist in this diff, so that specific check always silently returns `False` and the curve alert will always render in advisory wording even when actually armed. Minor, cosmetic, not a safety issue — flagged for whoever picks up the install-readiness work.
